# История изменений

[English version](CHANGELOG.md)

Версия в `pyproject.toml`, git-тег и релиз на GitHub всегда говорят одно и то же;
workflow релиза отклонит тег, который расходится с `pyproject.toml`, и тег, для
которого нет раздела ни в одном из двух changelog'ов.

## 0.20.0 - 2026-09-05

### Изменено

- **`duration` теперь один и тот же виджет на всех девяти нодах и принимает оба
  числовых типа.** У двух нод был слайдер `FLOAT` с точностью до десятой
  секунды, у остальных семи - поле `INT`, упиравшееся в 15. Сокет везде стал
  `FLOAT,INT`, так что `INT`-примитив, нода, считающая кадры, и `FLOAT` с
  математической ноды подключаются напрямую, без конвертера между ними, а сам
  виджет на каждой из девяти - вещественный, с точностью до десятой.

  Десятые и раньше были правдой. Все руководства пака пишут времена склеек до
  тысячных - `At 00:02.378, the camera cuts to`, - и все шаблоны уже
  форматировали число как `{float(duration):g}s`, то есть длина в 7.5 секунды
  проходила насквозь целой, и отказывался её произнести только виджет. Два места
  по дороге обрезали её через `int()`; больше не обрезают. Длительность - это
  подсказка, которую модель читает одной строкой текста, а не настройка: сколько
  клип продлится на самом деле, решают ниже по графу.

- **Потолка в 15 секунд больше нет, и докуда дотягивается виджет - решать вам.**
  MiniMax-H3 растягивают на двадцать и тридцать секунд и дальше пайплайнами,
  которых здесь никто не писал, а виджет, отказывающийся произнести 20, - это
  виджет, спорящий с тем, что и так на экране. Сервер принимает до 600 секунд на
  каждой ноде; виджет предлагает 30, пока вы это не поменяли.

### Добавлено

- **Меню `duration` на каждой ноде, у которой есть этот виджет.** Правый клик по
  ноде - на классическом холсте прямо по виджету, тогда пункт встанет первым -
  открывает подменю из двух пунктов: **Default value**, возвращающий виджету
  десять секунд, и **Longest offered...**, спрашивающий число и делающий его
  верхним краем виджета. Введённое округляется до десятой секунды, зажимается
  тем, что примет сервер, и хранится в собственном свойстве ноды `max_duration`,
  то есть сохраняется вместе с воркфлоу и возвращается с ним.

  Диапазон виджета фиксируется при объявлении ноды, и ни один диапазон не
  подходит всем графам сразу: руководство MiniMax написано вокруг клипов в
  несколько секунд, а те самые растянутые пайплайны уходят далеко за это.
  Поэтому сервер берёт всё разумное, а виджет охватывает то, с чем работаете вы.
  Слайдер, дотягивающийся до десяти минут, - это слайдер, который не подвинуть
  на 9.

  У Universal Writer это свойство `max_duration` есть с 0.14.0, но добраться до
  него можно было только через Properties Panel. Свойство осталось тем же самым,
  и панель до него по-прежнему достаёт; меню - это путь к нему, для которого не
  нужно знать, что он существует, и у остальных восьми нод теперь есть и то, и
  другое.

- **Список моделей теперь окно, а не файл, который открывается в блокноте.**
  Кнопка **Model list** на каждой ноде, грузящей модель, открывает панель поверх
  графа: модели, которые предлагает эта нода, с кнопками **Add a model**,
  **Edit** и **Delete** рядом. Показываются только те списки, которые нода
  действительно читает: Universal Rewriter открывается на трёх вкладках,
  капшенер - на одной, потому что адаптеры рассчитаны на разные архитектуры и
  запись из одного списка не загрузится в ноде другого. Под вкладками написано,
  чего список требует: архитектура, число блоков и ширина, нужен ли проектор и
  какие энкодеры он обязан нести.

  Всё по-прежнему лежит в том же `models.json`, и **Open models.json** в подвале
  по-прежнему его открывает - ради двух вещей, которые окно намеренно не
  трогает: секций `adapters` и пути к сетевой шаре. Модели, найденные в ваших
  папках моделей ComfyUI, тоже показаны, приглушённо и без правки: они попадают
  в выпадающий список, и их отсутствие в редакторе читалось бы как что-то, что
  окно потеряло.

  Добавление и удаление модели обновляет выпадающие списки в открытом графе
  сразу - на всех нодах, которые кормятся этим списком, и внутри подграфов, без
  перезагрузки браузера. Списки приходят с сервера, собранные теми же функциями,
  что вызывает `INPUT_TYPES`, поэтому это те же подписи в том же порядке, что
  вернул бы следующий `/object_info`.

- **Проверка, которая читает модель до того, как вы потратите на неё загрузку.**
  **Check it** в форме добавления и правки отвечает настолько, насколько можно
  ответить, не двигая веса. Файл, уже лежащий на этой машине, читается напрямую:

  ```text
  - 'Qwen2.5-Omni-7B-Q4_K_M.gguf' is a 'qwen2vl' model, 28 blocks of width 3584. That fits.
  - 'mmproj-Qwen2.5-Omni-7B-Q8_0.gguf' carries the vision and audio encoder.
  ```

  Предложите тот же файл списку 27B - и вам скажут об этом до сохранения.
  Репозиторий transformers на Hugging Face оценивается по `config.json` в 4 КБ, а
  репозиторий GGUF - вопросом, есть ли в нём названные вами файлы. Именно это
  ловит опечатку в `file` или `mmproj`, до сих пор бывшую загрузкой, которая
  падала через несколько минут, и заодно заполняет `download_gb`.

- **Restore the packaged entries.** `seed_offered` - это то, из-за чего удаление
  держится через обновления, а удаление списка оттуда возвращает записи пака.
  Блок комментариев в файле всегда об этом говорил; теперь это кнопка. Ваших
  собственных записей она не касается.

- **Модели, уже скачанные вами для Ollama, предлагаются писателями и
  капшенерами** (issue #12). Они появляются в списках `writer_model` и
  `caption_model` с префиксом `ollama:` и под теми же именами, что показывает
  `ollama list`, - `ollama: qwen3:8b [qwen3, 4.7 GB]`, - и ничего для этого не
  копируется, не конвертируется и не скачивается.

  То, что скачивает Ollama, - обычный GGUF, и llama.cpp читает его там, где он
  лежит. Читается файл, а не API Ollama: сервис можно остановить, убрать из
  автозапуска или снести, модели останутся рабочими. Мультимодальная модель
  попадает в оба списка - писателем без проектора и капшенером вместе с ним, - а
  пару даёт манифест самой модели, где оба файла названы рядом; это достоверно в
  отличие от сравнения имён файлов в папке.

  Автоматически находятся три хранилища: `OLLAMA_MODELS`, если вы его задали,
  `~/.ollama/models` и `/usr/share/ollama/.ollama/models` для установки
  сервисом. Хранилище где-то ещё - внутри WSL, в контейнере, на диске, о котором
  сервер не знает, - указывается руками через `ollama_stores` в `models.json`
  или через `MINIMAX_H3_OLLAMA_MODELS`. Автоматически они не ищутся намеренно:
  обращение к пути вида `\\wsl$\...` поднимает остановленный дистрибутив WSL, а
  эти списки строятся заново при каждом заполнении выпадающего списка. Открытие
  вкладки ComfyUI не должно запускать виртуальную машину.

### Исправлено

- **Запись Omni без `mmproj` падала после загрузки, а не до неё.** `models_8b`
  пропускает GGUF-запись без проектора и пишет об этом в лог; `models_omni` её
  предлагал, и прогон умирал на `'x.gguf' and '' were not both present after
  downloading`, когда гигабайты уже потрачены. Теперь пропускает так же, а окно
  списка моделей вообще не даёт такую записать.

- **Удалённая модель могла вернуться при следующем запуске.** Слияние
  записывает, что пак вам уже предлагал, только когда работает над файлом,
  который смогло разобрать, а поставляемый `models.json` такой записи не несёт
  вовсе - поэтому удаление, сделанное до того, как это хоть раз произошло,
  отменялось следующим чтением. Удаление через окно записывает имя само.
  Записываются только имена, которые пак действительно поставляет: `seed_offered`
  ваш, и выдуманные имена в нём молча закрыли бы дорогу будущей одноимённой
  записи пака.

- **Две правки внутри одной секунды могли показать более старую.** Разобранный
  список кэшируется по `(путь, размер, целые секунды)`, а замена заметки о VRAM
  с «8 GB» на «9 GB» не двигает ни размер, ни секунду. Правка руками до этого не
  доходила, окно доходит. Теперь кэш сбрасывает сама запись.

## 0.19.1 - 2026-09-04

### Исправлено

- **Рерайтер мог вернуть вместо промта четыре сломанных токена, и квантизация
  тут была ни при чём.** На запрос переписать "The cat" с Omni LoRA в int8 он
  возвращал девять символов - слово "int", перевод строки, слово "Human" - и
  останавливался. Та же нода, тот же сид, те же веса теперь возвращают 1871
  символ, все три поля на месте.

  Виновата настройка torch, которая принадлежит процессу, а не этому паку.
  Импорт `comfy.model_management` включает
  `allow_fp16_bf16_reduction_math_sdp` для всего, что работает внутри ComfyUI, и
  включает безусловно - это не один из ключей `--fast`, и отказаться от неё для
  отдельной модели из воркфлоу нельзя. Для диффузионной модели это честный
  размен: она укороченного накопителя не замечает. Языковая модель на 7B,
  пишущая страницу структурированного текста, замечает: там, где flash- и
  memory-efficient-ядра внимания отказываются от такой формы и torch падает в
  math-ядро, редукция считается в bf16, первый шаг декодирования выбирается
  неверно, а из жадного декодирования после неверно выбранного первого токена
  возврата нет.

  Дело было не в квантизации. С включённой настройкой ломаются и nf4, и int8, и
  bfloat16, а с выключенной все три пишут один и тот же ответ; nf4 была мягкой
  половиной той же поломки - отсюда слипшиеся слова и незакрытые скобки. Этим же
  объясняется, почему рассказы об этом расходились: увидит машина проблему или
  нет, зависит от того, какое ядро внимания достанется её карте для этой формы,
  и карта, которой достаётся flash или cuDNN, не увидит её никогда.

  Генерация теперь держит редукцию выключенной и возвращает переключатель ровно
  в том виде, в каком его нашла, - собственные модели ComfyUI сохраняют нужную
  им настройку. Это покрывает все три трансформерных адаптера: 27B, 8B и Omni.
  GGUF-движки задеты никогда не были: llama.cpp считает своими ядрами и ничего
  из этого не читает.

### Добавлено

- **Генерация, ушедшая в повтор, останавливается, и self-check об этом
  говорит.** Раньше цикл здесь не видел никто. Ответ, начавший писать "0000000"
  или "shot 1 shot 1 shot 1", доходил до `max_new_tokens` и уходил дальше по
  графу как готовый промт; единственным признаком была находка self-check о
  полях, до которых ответ не добрался, вместе с советом про температуру,
  которую жадное декодирование не читает.

  На трансформерном движке слежение идёт по токенам, а не по тексту: шесть
  точных повторов блока длиной до 32 токенов, проверка каждый шестнадцатый шаг и
  не раньше 96 написанных токенов, причём окно берётся только из ответа - ничто
  из повторов самой инструкции счёт начать не может. Это обязано быть по
  идентификаторам: стример отдаёт только то, что до последнего пробела, так что
  прогон без пробелов внутри приходит вообще ничем - а это ровно худший случай.

  Движки llama.cpp получают то же слежение по тексту, и там это правомерно,
  потому что llama-cpp-python отдаёт текст каждого токена по мере его
  появления. `runner.run` и серверный `ask` теперь считаются с тем, что вернул
  их текстовый колбэк, так что ушедшее в повтор описание тоже обрывается.
  Остановка не считается сбоем: написанное до неё сохраняется и возвращается,
  как любой другой ответ.

  Self-check затем называет это - `the answer gets stuck repeating "shot 1" for
  1,500 characters and gets no further` - как факт, а не как совет: то же
  правило читает промты, набранные в библиотеке, где никакая модель не
  запускалась и крутить нечего. Покрыты две формы, а не общая теория явления:
  постоянная единица длиной до 120 символов, циклящаяся в конце, и один символ,
  идущий подряд 80 раз и больше. Предложение, возвращающееся каждые 137
  символов, за эту границу выходит, а у сноса вида "shot 1 shot 2 shot 3"
  точного периода нет.

  `fix_once` больше на этом не сдаётся. Два пропущенных поля из трёх раньше
  читались как ответ, слишком далёкий от формата, чтобы его спасать; на цикле
  всё наоборот - модель держала формат безупречно вплоть до момента, когда
  пошла на повтор, - так что одну попытку потратить стоит. В промт при этом
  уходит правило, а не сама находка: процитировать модели "000000" обратно
  значит вручить ей цикл для продолжения.

## 0.19.0 - 2026-09-04

### Добавлено

- **Тысяча готовых промтов MiniMax-H3 теперь идёт вместе с паком.** Нода
  "MiniMax-H3 Prompt Presets" отдаёт один из них. Окно за её кнопкой отбирает по
  манере съёмки, теме, форме кадра, числу планов и наличию реплик, ищет по
  словам и показывает кадр того клипа, под который промт написан; клик по кадру
  проигрывает клип.

  Это готовые T2VA-промты в том самом формате, который пишут писатели этого пака
  - те же три поля с метками, - так что `prompt` идёт прямо в генератор или во
  вход `prompt` писателя, чтобы переписать от него. Этот выбор - провод, а не
  виджет, и именно поэтому здесь отдельная нода, а не ещё одна кнопка в окне
  библиотеки. Остальные выходы: те же три поля по отдельности, размер и
  длительность клипа и строка о происхождении.

  Ничего не скачивается ни при установке, ни во время прогона. Промты, их теги и
  по одному кадру в 256 пикселей на каждый - это 6 МБ внутри пака, читаемые с
  диска и распаковываемые при первом обращении; прогон занимает единицы
  миллисекунд. В сеть уходит единственное - клип в предпросмотре, и только по
  клику на кадр: сперва huggingface.co, а если тот не отвечает, hf-mirror.com,
  то есть адрес, который отвечает из материкового Китая.

  Промты и клипы, которые они описывают, - работа ostris, перенесённая сюда с
  указанием авторства и без перелицензирования:
  [ostris/minimax_h3_1k](https://huggingface.co/datasets/ostris/minimax_h3_1k).
  Теги манеры съёмки и темы - это прочтение подборки проектом
  [H3 Atlas](https://cohub.live/baize/video-altas/w/h3-atlas); кадры нарезаны из
  тех клипов здесь. Все до единой тысячи проходят собственную самопроверку пака
  без единой находки.

- **Любой из них можно скопировать в свою библиотеку промтов.** Кнопка "Save to
  the library" открывает тот же редактор, которым библиотека пользуется и так, -
  имя, описание, группы, сам текст и проверка по мере набора, - и кладёт копию в
  тот набор промтов, который вы выберете.

  Копия говорит в описании, откуда она взялась, так что промт, ушедший дальше,
  по-прежнему называет, чей он, а дальше это обычная запись: правится, лежит в
  ваших группах и доступна любому писателю через окно библиотеки. Встроенная
  заготовка при этом не трогается, и нода продолжает отдавать оригинал.

### Изменено

- **Вместо одного примерного воркфлоу теперь шесть.** Раньше в паке лежали
  сложенные в один граф штатные шаблоны ComfyUI Ref2VA и FL2VA: 59 нод, 89
  связей, сохранено на масштабе 19%, семь нод в bypass при открытии - включая
  само поле ввода промта в нижней половине, - и одно нажатие Run запускало обе
  половины сразу, два диффузионных чекпойнта и текстовый энкодер на 32B, ещё до
  того, как на экране появлялось что-то читаемое. Из четырнадцати нод пака в нём
  было три, и ноды, которой пак назван, среди них не было.

  На их месте - шесть карточек в том же браузере, пронумерованных в том порядке,
  в каком с ними стоит знакомиться. Четыре пишут промты и не грузят чекпойнт
  MiniMax-H3 вовсе: маленький писатель, 27B-адаптер, писатель по референсам и
  встроенные заготовки. Два генерируют, и это штатные шаблоны из галереи ComfyUI
  в нынешнем виде - генератор свёрнут в один субграф, как их теперь и
  поставляют, - с писателем и проверкой промта перед ним. Десять и тринадцать
  нод вместо пятидесяти девяти.

  Каждый из шести открывается на масштабе, на котором его видно, ничего не
  отключает при открытии, заканчивает каждую цепочку тем, что показывает
  результат, и несёт заметку "Read me first": что он делает, что скачивает, что
  выставить перед запуском и какой шаблон открыть следующим. Загрузчики картинок
  идут пустыми, а не с именами файлов, которых ни у кого больше нет.

- **`tools/template_cards.py` рисует картинки, которые показывает браузер
  шаблонов.** Карточку пака ComfyUI собирает из одного имени файла, а картинку
  ищет по адресу `<имя>.jpg` рядом с воркфлоу, так что своя нужна каждому
  шаблону. Скриншот не годится: поля промтов, заметки и превью - это HTML поверх
  канвы, и на снимке они пустые. Карточки вместо этого рисуют цепочку нод, синим
  - если шаблон только пишет текст, янтарным - если ему нужны веса видео.

## 0.18.6 - 2026-09-01

### Добавлено

- **Прогон с несколькими референсами грузит каптионер один раз, а не по разу
  на каждый.** `llama-mtmd-cli` запускает процесс на каждое описание: для одной
  картинки это верно, для шести - расточительно, потому что модель и проектор
  перечитывались с нуля всякий раз. Теперь на весь цикл держится один
  `llama-server`, и его спрашивают по ассету. Та же модель, тот же проектор, та
  же выборка, тот же сид и тот же системный ход - только загрузка одна. Три
  референса на каптионере 8B при прогретом файловом кэше: было 8.7 с, стало
  3.6 с, и выигрыш растёт вместе с полоской.

  Каждый путь повторяется точно: один и тот же граф на одном и том же сиде даёт
  один и тот же капшен, на любом из двух. А между собой они могут разойтись на
  слово - "a blue circle centered on" против "a blue circle is centered on", -
  там, где два токена почти равны и ничью решает то, как рантайм батчит. Это
  арифметика с плавающей точкой, а не другой заданный вопрос.

  Это ускорение, а не требование. Сборка без `llama-server` рядом с
  каптионером, порт, который не поднялся, сервер, так и не сказавший, что
  здоров, - каждый случай пишется в лог, и прогон продолжается по процессу за
  раз. Один референс сервера не поднимает вовсе: экономить нечего. Для машины,
  где это ведёт себя плохо, есть `MINIMAX_H3_MTMD_SERVER` со значениями `auto`,
  `never` и `always`.

  Ничего не скачивается. `llama-server` лежит в том же архиве релиза, что и
  `llama-mtmd-cli`, поэтому он ищется рядом с уже найденным каптионером - и
  только там: сервер из другой сборки мог бы свести модель со старым mtmd,
  который не прочитает только что принятый проектор.

- **Новая нода принимает референсы, приехавшие вместе, и отдаёт их по одному на
  сокет.** "MiniMax-H3 Reference Adapter" читает батч картинок, список от
  другой ноды или связку референсов из чужого пака, выясняет род каждого
  элемента по самому значению и кладёт его на выход картинки, клипа или звука -
  то есть в тот вид, который принимают писатели, так что полоска референсов под
  ними работает как работала.

  Раньше это было невозможно в принципе: у ноды, отдающей референсы собранными,
  нет ничего, что мог бы принять вход "по ассету на слот", а чтобы получать
  настоящий список ComfyUI, нужен флаг `is_input_list`, переписывающий форму
  всех входов ноды, на которой он стоит. Поэтому флаг живёт на отдельной ноде -
  ровно в этом и причина, почему это нода, а не вход.

  Девять картинок, три клипа и три звука - потолок Ref2VA, столько места и
  есть; всё сверх попадает в выход summary, а не пропадает молча. Незанятые
  выходы отдают пустоту, а писатель такие и так пропускает, так что подключить
  сокет, оказавшийся пустым, ничего не стоит. Батч картинок по умолчанию
  разбирается на референс за кадр; `split_batches` это выключает, когда кадры -
  это один шот, увиденный несколько раз.

### Изменено

- **Описание теперь задаёт системный ход явно, а не опускает его** - и от этого
  меняются капшены после обновления. "Опущен" - это не одно состояние: без
  системного хода `llama-mtmd-cli` и `llama-server` строят разные разговоры и
  отвечают по-разному. На Qwen2.5-Omni-3B при одном сиде и одной выборке первый
  описал синусоидальный свип как "a variety of synthesized sounds, including a
  sine wave, square wave, and a sweeping tone", второй - как "a descending
  sweep"; верен был второй. Ни одно значение системного промта не воспроизводит
  дефолт одной программы в другой; совпадают они тогда, когда ход задан обеим,
  поэтому он задаётся обеим.

  Заодно это забирает ответ у дефолтов двух чужих программ, которые вправе
  поменяться в очередном релизе llama.cpp, не называя это изменением. Писатели
  8B и Omni передают собственный системный промт и не затронуты; каптионинг
  моделью, уже загруженной ComfyUI, тоже.

- **Ни один потомок llama.cpp больше не переживает ComfyUI, чем бы тот ни
  кончился.** Хук `atexit` срабатывает только при штатном выходе, поэтому
  падение, убийство из диспетчера задач или остановка из консоли оставляли
  потомка с целыми гигабайтами VRAM. Теперь каждый потомок ещё и в job object,
  умирающем вместе с этим процессом (Windows), и получает `PR_SET_PDEATHSIG`
  (Linux). Проверено убийством родителя тем же способом, каким его убивает
  падение: потомок исчезает за секунду, а без этого живёт дольше пятнадцати.

## 0.18.5 - 2026-09-01

### Добавлено

- **Новая нода читает промт, написанный где-то ещё.** "MiniMax-H3 Prompt Check"
  принимает текст на сокет и применяет ровно те правила, которые писатели
  применяют к собственным ответам: нумерация планов, времена склеек против
  длительности, разметка реплик, теги референсов против того, что задача может
  принять и что действительно подключено, - а потом разбирает текст на те же
  поля. Кормить её может всё, что производит прозу MiniMax-H3: нода другого
  пака, загруженный в граф текстовый файл, промт из сети или набранное руками.

  Никакая модель не грузится и ничего не генерируется, прогон стоит несколько
  миллисекунд. Что вошло, то и выходит из первого выхода нетронутым, так что
  нода может стоять в середине графа. Последний выход несёт находки и несёт их
  всегда все: `self_check` управляет тем, что сказано на экране, а это вопрос о
  шуме - подключить же выход значит спросить. "Save the last prompt" на ней тоже
  есть: промт, пришедший со стороны и прочитавшийся чисто, ложится в библиотеку
  наравне с написанным этим паком.

- **Ответ, который держит нода, можно править.** Кнопка "Edit the last prompt"
  рядом с "Save the last prompt" на каждом писателе открывает его в том же
  редакторе, которым уже пользуется библиотека: промт в поле, где можно писать,
  и чтение по мере набора - по тем правилам, по которым читался прогон. Обе
  делят одну строку: они работают с одним и тем же, а колонка из пяти кнопок во
  всю ширину ничего не говорила о том, какие из них родня.

  При сохранении поля заново выделяются из нового текста, так что выходы-секции
  остаются в согласии с прозой; всё, что нода сохранила после полей, - блок
  референсов, список описаний - принадлежит прогону, а не прозе, и остаётся как
  было. На диск не попадает ничего: это сессионное хранилище, и правку, которую
  стоит сохранить, потом кладут в библиотеку. Капшенеры тоже держат запись, и их
  запись правку отклоняет, а не калечит: строка об одном ассете - не промт с
  полями.

### Изменено

- **У README появилось содержание.** Две тысячи строк без карты - это страница,
  которую листают, а не читают. В нём каждый раздел и подраздел, под теми
  якорями, которые им и правда даёт GitHub.

- **Ref2VA отклоняет переполненную полосу до того, как что-либо загрузится.**
  Задача, которой не годится подключённое, отклонялась и раньше, прежде чем
  сдвинется хоть один вес; кадровая задача - да, а референсная нет, потому что у
  неё нет фиксированного числа картинок, только потолок: девять картинок, три
  клипа, три звука. Превышение сперва описывалось, а помечалось потом - полный
  проход каптионера и переписывание ради того, что можно сосчитать заранее.
  Субъекты считаются отдельно от картинок, так что бейдж часто и есть всё
  лечение.

- **Ключ кэша у писателей теперь покрывает и тот ответ, который они держат**, а
  не только запись библиотеки, на которую они наведены. Ни та ни другая правка
  не меняет входов ноды, так что без этого ComfyUI продолжал бы отдавать ответ,
  каким он был до правки. При выключенном `repeat_last` значение постоянно, и
  кэширование ровно такое, каким было.

- **`tools/locales.py` говорит, когда не может прочитать имя, под которым
  зарегистрирована нода**, вместо того чтобы её пропустить. Имя, собранное из
  константы, невидимо для чтения AST, и пропущенная так нода никогда не
  предлагается к переводу - молча. Именно так нода проверки чуть не уехала без
  перевода.

### Исправлено

- **"Save the last prompt" открывала окно, в котором нечего было делать.** Когда
  ответа нет, в этом окне мертво всё, а строка внизу объясняла почему, - тогда
  как "Edit the last prompt", задавая ровно тот же вопрос, отказывала, ничего не
  открыв. Теперь обе отказывают сразу и из одного места, и каждая говорит то, в
  чём легко ошибиться: промт, отданный из библиотеки, никогда не был собственным
  ответом этой ноды, поэтому здесь он и не хранится - он уже в библиотеке, под
  своим именем.

- **Нода, отклонившая прогон, теперь говорит об этом на экране.** Любой отказ -
  задача, которой не годится подключённое, полоса, где больше, чем H3 способен
  принять, - был только исключением: он останавливал прогон и уходил в консоль
  трейсбеком, а туда смотрят не первым делом. Та же фраза теперь идёт тостом,
  прежде чем исключение уйдёт наверх, под меткой **Stopped**.

- **Тосты держатся достаточно, чтобы их прочитали.** Восьми секунд хватало на
  одну короткую строку и близко не хватало на четыре находки. Время жизни
  теперь берётся из объёма текста - примерно 45 мс на знак, не меньше десяти
  секунд и не больше тридцати.

- **Редактор промта говорит, дойдёт ли правка до выхода.** Правка того, что
  держит нода, сама по себе не делает ничего: сохранённый промт главнее
  собственного ответа ноды, а при выключенном `repeat_last` следующий прогон
  пишет новый ответ поверх правки - и на жадном сиде байт в байт тот же текст,
  так что правка выглядит молча проигнорированной. Теперь редактор говорит, что
  именно стоит на пути, ещё до того, как вы начали печатать; сохранение
  включает `repeat_last`, если больше ничто не мешает; а тост после сообщает,
  что произойдёт на самом деле.

- **Надпись на самой кнопке не появлялась, пока что-нибудь ещё не перерисует
  эту ноду.** "Saved to global" и подобные ставились на виджет и там оставались:
  перерисоваться просили граф, а ноду - нет, и подтверждение всплывало позже,
  прицепившись к постороннему щелчку.

  Каждая кнопка пака теперь живёт в общем виджете-строке, а не в отдельном
  канвасном виджете. Это и позволяет двум встать рядом, и даёт им всем один
  вид - безразлично, одна кнопка в строке или две, - и заставляет мимолётную
  надпись появляться в тот же миг, когда её написали: текст живёт в документе,
  и просить перерисоваться некого. "Open model list" и "Open guide folder"
  делят строку по той же причине, что Save и Edit, а сами строки стоят теснее
  остальных органов управления пака: это один блок того, что можно сделать с
  нодой, а не четыре несвязанных. Вид задан один раз, в `mmx_controls.js`,
  вместе с остальными органами управления пака.

- Отказы от "Edit the last prompt" стали тостами, а не двухсекундной надписью:
  лицо кнопки не того размера, чтобы нести причину, и не то место, куда
  смотришь, когда ждал окна. Тот, что про пустую память, говорит заодно вещь,
  в которой легко ошибиться: промт, отданный из библиотеки, никогда не был
  собственным ответом этой ноды, и правится он в окне библиотеки.

## 0.18.4 - 2026-09-01

### Добавлено

- **Сохранённый промт теперь правится там же, где хранится.** У каждой карточки
  в окне библиотеки рядом с Use, Copy и Delete появилась кнопка **Edit**: она
  открывает имя записи, описание, группы и сам промт в поле, в котором можно
  писать. То, чем запись сделана, - писатель, настройки, длительность,
  миниатюры референсов - показано, но не правится и не меняется: эта половина
  есть отчёт о прогоне, а карточка, совравшая о собственном происхождении, была
  бы хуже, чем никакой.

- **Самопроверка читает набираемый текст по мере набора.** Редактор гоняет те же
  правила, которыми писатели читают ответ модели, и тем же самым модулем, а
  задачу, длительность и референсы для проверки даёт сама запись, - так что
  время склейки, уехавшее за конец видео, будет названо, пока вы ещё на него
  смотрите. Ради этого и стоит править здесь, а не в JSON-файле: файл примет что
  угодно. `self_check` на ноде Options управляет тем, что ноды говорят во время
  прогона; редактор показывает все находки всегда - вы попросили о них, открыв
  окно.

  Изменение текста снимает с записи авторскую разбивку ответа на поля: она была
  сделана из текста, каким он был. Без неё читающая нода разбирает текст сама -
  ровно то, что она уже делает с промтом, написанным нодой другого типа, - и
  выходы-секции остаются в согласии с тем, что вы написали. Правка, ничего не
  изменившая, не пишется вовсе, а настоящая ставит на запись время изменения,
  которое карточка показывает рядом со временем сохранения.

  За этим стоят `library.edit` и `library.inspect`, маршруты `library/update` и
  `library/check`, а тесты на них в `tests/`:
  `cd tests && ../.venv/Scripts/python.exe -m pytest`.

### Исправлено

- **Нода, наведённая на сохранённый промт, теперь замечает, что тот изменился.**
  Запускать ли ноду вообще, ComfyUI решает по её входам, а правка записи не
  меняет ни одного из них - выбор всё тот же id в том же файле, - и ответ
  возвращался прямо из кэша выполнения, по-прежнему говоря то, что говорил до
  правки, и ничто на экране этого не объясняло. Теперь писатели сообщают в
  `IS_CHANGED` (на нодах v3 - `fingerprint_inputs`) содержимое самой выбранной
  записи, так что правка заставляет ноду отработать и отдать то, что там теперь
  написано. Больше в кэшировании не меняется ничего: когда сохранённый промт не
  задействован, значение постоянно, а переименование записи или переписанное
  описание не заставляют перезапускаться все ноды, что её держат. Выбор, чью
  запись удалили в другом месте, теперь отрабатывает и ругается, а не отдаёт
  тихо ответ из записи, которой больше нет.

- **Окно "New prompt file" оставляло обещание неразрешённым, если его закрыли**,
  а не ответили, - через Escape или щелчком мимо. Видимых последствий не было,
  но вызывающая сторона ждала вечно. Теперь закрытие любого из этих окон
  отвечает вызывающему так же, как Cancel.

- **Escape доставался всем открытым окнам сразу.** Пока окно было одно, это не
  проявлялось; редактор открывается поверх библиотеки и закрыл бы оба. Панели
  теперь помнят собственный порядок, и Escape закрывает только верхнюю. А
  щелчок мимо редактора не закрывает его вовсе: промт стоит сотен слов набора,
  а в подложку легко попасть.

## 0.18.3 - 2026-08-30

### Добавлено

- **`self_check` на ноде Options решает, сколько проверка произносит
  вслух**: `warnings and notes` (по умолчанию), `warnings only` -
  отбрасывает мягкие советы руководства вроде 350-500 слов - или
  `off`. Чтение происходит всегда, оно ничего не стоит, поэтому
  переключатель решает, о чём сообщать, а не на что смотреть; нечитаемое
  значение сообщает всё: устаревший воркфлоу должен оставить проверку
  громче задуманного, но не немой. Собственных предупреждений нод о их
  входах он не покрывает - те про вашу проводку, а не про прозу модели.

- **`fix_once` на ноде Options действует по находкам**, а не только
  сообщает о них. По умолчанию выключен, и выключенный он - обещание:
  ответ, который вы получите, ровно такой, каким его написала модель.

  Включённый возвращает на место строку выравнивания кадровых задач, если
  её нет, - это фиксированный текст, который нода и так формирует, никакая
  модель не запускается и регрессировать нечему, - а механические находки
  покупают ровно одну лишнюю генерацию, с ними же, вложенными в промт
  как правила. Один раз, не цикл. Повтор не запускается на ответе, где нет
  половины полей: модель, не удержавшая формат, не удержит его и со второго
  раза. Правила едут внутри промта, а не вторым ходом диалога, - так
  сохраняется однооборотная форма, на которой обучались LoRA. Второй ответ
  оставляют, только если он действительно лучше; ничья - за первым. Худший
  случай - потраченная минута, но никогда не худший промт. Что именно
  произошло, сказано на ноде.

  Что ищется - в `minimax_h3_rewriter/checks.py`, что с этим делать - в
  `minimax_h3_rewriter/repair.py`, оба без ComfyUI, а тесты на них в `tests/`:
  `cd tests && ../.venv/Scripts/python.exe -m pytest`.

Оба виджета дописаны после `prompt_file`, поэтому воркфлоу, сохранённый до
этого релиза, сохраняет все остальные значения на своих местах.

- **Русская локаль.** Переключите ComfyUI на русский - и ноды поднимутся
  по-русски: что такое каждая нода и все 153 подсказки к виджетам, то есть
  длинные объяснения, которые вы и читаете.

  Имена виджетов намеренно оставлены английскими. Это идентификаторы, а не
  проза: ключи в JSON воркфлоу и в вызове API, то, как вещи называются в README
  и в issue, и то, чем подсказки называют виджеты. Перевод подписи оставил бы
  все эти ссылки указывающими в никуда - а так русская подсказка говорит
  `repeat_last`, и это ровно то, что на ноде. `device` тоже оставлен
  по-английски: он собирается на лету и заканчивается списком видеокарт машины,
  а статический файл такого нести не должен.

  Переводы лежат в `locales/<язык>/nodeDefs.json` в корне пака; добавить язык -
  значит добавить папку. `python tools/locales.py report ru` говорит, чего не
  хватает и что устарело, спрашивая у запущенного ComfyUI, какие ноды есть на
  самом деле, а не вторую копию определений, которая разошлась бы; `fill`
  дописывает недостающие ключи по-английски, не трогая уже переведённые. Тесты
  закрывают то, что ломается молча: ключ, которого ComfyUI не читает, ноду
  чужого пака, пустую строку, файл, который не JSON.

  Нода, уже стоящая на холсте, сохраняет заголовок, с которым её создали: он
  записан в воркфлоу, и смена языка её не переименует.

## 0.18.2 - 2026-08-30

### Добавлено

- **Ответ проверяется.** Каждый свежий ответ, прежде чем покинуть ноду,
  перечитывается по правилам собственных руководств MiniMax: нумерация шотов и
  таймкоды против заданной длительности, разметка диалогов и её языковые теги,
  теги референсов против того, что задача может нести и что реально
  подключено, строка retention для каждого введённого субъекта, ориентир в
  350-500 слов и фиксированная строка выравнивания кадровых задач. Находки
  сообщаются в трёх местах - тостом в окне ComfyUI с первыми из них, подписью
  ноды с полным списком над ответом и консолью, - и ничего не блокируется:
  модель иногда права, нарушая правило, и только вы знаете, тот ли это случай.
  Чистый ответ не добавляет ни строки. Промты из библиотеки не
  перепроверяются: их проверили, когда они писались.

  Правила живут в одном чистом модуле, `minimax_h3_rewriter/checks.py`, с
  тестами в `tests/`, которым не нужен ComfyUI:
  `cd tests && ../.venv/Scripts/python.exe -m pytest`.

- **Плашки `bypass` и `repeat_last` теперь всегда на заголовке**, а не только
  у свёрнутой ноды или при включённом переключателе. У высоких райтеров сам
  виджет - в экране прокрутки отсюда, а плашка, которая появляется и исчезает,
  двигает соседнюю; два бейджа стабильным рядом - это переключатель, по
  которому можно попасть не скролля, развёрнута нода или нет.

- **Собственные предупреждения нод тоже стали тостами**, под заголовком
  "Heads-up" вместо "Self-check": референс, который выбранная задача не читает
  (лишний кадр у 8B, непрочитанные кадры 27B-вкладки, игнорируемые референсы
  T2VA у Universal Writer), сохранённый промт, чьи референсы разошлись с тем,
  что ноде показывают, и капшенер, вернувший пустоту для подключённого ассета.
  Всё это и раньше говорилось на ноде и в консоли - теперь это ещё и
  невозможно пропустить.

## 0.18.1 - 2026-08-30

### Добавлено

- **Готовый воркфлоу в комплекте** (issue #10). В `example_workflows/` лежит
  собранный граф, который браузер шаблонов ComfyUI показывает под именем этого
  пака: штатные шаблоны MiniMax-H3 Ref2VA и FL2VA с Universal Rewriter и
  Universal Writer, подключёнными перед каждой веткой. Чекпойнты в нём
  штатные; недостающие ComfyUI предложит скачать.

- **Qwen3.8-27B в каталоге райтеров.** `unsloth/Qwen3.8-27B-GGUF` добавлена в
  список guided-райтеров в четырёх динамических квантах unsloth - Q2_K_XL
  (9.2 ГБ, карта на 16 ГБ), Q3_K_XL (12.2 ГБ), Q4_K_M (15.3 ГБ, карта на
  24 ГБ) и почти без потерь Q6_K_XL (23.6 ГБ, карта на 32 ГБ) - самая сильная
  проза в списке на каждом из этих размеров. Райтеры - её законное место: у
  Qwen3.8-27B 65 слоёв, а LoRA 27B-рерайтера обучена на 64 слоях Qwen3.6-27B,
  так что адаптер на неё не сядет - базой рерайтера она бежала бы как обычная
  модель. Записи доливаются в `models.json` существующих установок обычным
  мерджем; ваши правки не трогаются.

- **Сканы моделей теперь смотрят в `text_encoders` и `clip`, а скан адаптеров -
  ещё и в `loras`.** GGUF-загрузчики самого ComfyUI кладут энкодеры Qwen-VL в
  `text_encoders` и `clip`, так что файлы у людей уже лежат там - а скан,
  который эти папки пропускал, выглядел как «не видит ни одной модели»
  (issue #11 и не одно сообщение после). `models/LLM` остаётся рекомендуемым
  местом; расширенный скан - для файлов, которые уже живут в других папках.
  Фильтры по архитектуре и размерам никуда не делись: новые папки добавляют
  подходящие модели, а не всё подряд - а guided-райтеры, принимающие любую
  архитектуру, теперь пропускают энкодерные половины картиночных пайплайнов
  и embedding-модели (`t5`, `t5encoder`, `bert`, `nomic-bert`,
  `nomic-bert-moe`, `clip`): без chat template им нечего сказать, а лежат они
  в каждой папке `text_encoders`.

### Исправлено

- **Воркфлоу, сохранённые до 0.17.2, снова загружаются с правильными значениями
  в Options** (issue #11). ComfyUI восстанавливает значения виджетов ноды по
  позиции, а 0.17.2 вставила `merge_lora` в середину списка, между `use_lora` и
  `auto_download`. Каждая нода Options, сохранённая раньше, возвращалась со
  сдвигом на один слот: в `merge_lora` оказывался boolean, в `gpu_layers` -
  8192 из `n_ctx`, `n_ctx` читал `auto` как NaN - и валидация отклоняла весь
  граф четырьмя ошибками, из которых ничего этого не видно.

  Теперь нода узнаёт старую раскладку при загрузке - boolean в слоте
  `merge_lora`, чего combo не сохраняет никогда, - и раздаёт значения обратно
  тем виджетам, из которых они были записаны, а `merge_lora` получает свой
  `auto` по умолчанию. Руками ничего переделывать не нужно; достаточно
  перезагрузить воркфлоу.

- **`duration` у Universal Rewriter теперь `FLOAT`, как у Universal Writer.**
  Как `INT` он отказывался принимать провод от любого float-источника секунд,
  который райтер принимал. Значение и дальше остаётся дробным: путь 8B раньше
  обрезал его на входе в запрос, а теперь `7.5` доходит как `duration: 7.5s`,
  при этом целые числа рендерятся ровно как раньше - `10s`, а не `10.0s`.
  Вкладка Omni дроби принимала и так и привязывает их к сетке кадров;
  отдельные ноды-рерайтеры сохраняют целочисленный виджет.

## 0.18.0 - 2026-08-29

### Добавлено

- **Библиотека промтов.** `repeat_last` из 0.17.3 держит по одному ответу на ноду
  и забывает его при перезапуске ComfyUI. Это вторая половина: промты, которым вы
  дали имя и оставили в JSON-файле в пользовательской папке ComfyUI, доступные
  любому воркфлоу и любой ноде.

  **Save the last prompt** на любом райтере или рерайтере открывает окно с
  именем, описанием и любым числом групп: группы, уже встречающиеся в файле,
  возвращаются чипами, новая добавляется словом и Enter. Сохраняется сам прогон -
  текст, секции после него, задача, модель, пропорции, длительность, seed и
  миниатюра 50x50 каждого референса, который видела нода.

  Миниатюры снимаются **во время прогона** - единственный момент, когда это
  возможно: к открытию окна нода уже вернула строки, а тензоров нет. Поэтому
  сессионная запись и библиотечная - одной формы, а сохранение сводится к тому,
  чтобы дать имя уже готовому. Записывается только то, что действительно
  существует: у картинки есть размер; у клипа, пришедшего настоящим `VIDEO`, есть
  кадры, секунды и частота кадров, а у пришедшего батчем картинок - только число
  кадров, потому что контейнера, у которого можно спросить, нет; у звука есть
  длительность, частота и число каналов. Референсы помечаются по позиции -
  `ref1-image`, `ref2-audio`, - а не по имени файла, которого нода не видит.

  **Prompt library** открывает список: сначала собственный последний ответ ноды,
  затем файл, новые сверху. Фильтр по группам чипами и поиск сразу по именам,
  описаниям, промтам, группам, референсам и настройкам. **Use** указывает ноде на
  запись, **Delete** удаляет её насовсем.

  Выбранный промт попадает в `rewritten_prompt` и в выходы-секции ровно так же,
  как свежий: модель не грузится, ничего не генерируется, прогон занимает около
  десятой доли секунды против полусотни, которые стоит настоящий рерайт. Запись,
  сделанная нодой другого типа, тоже работает: при совпадении классов выходы
  отдаются дословно, иначе текст разбирается на секции средствами читающей ноды -
  так же, как она разбирает написанное сама.

  Выбор лежит в скрытом виджете, поэтому сохраняется с воркфлоу и доходит до
  API-запуска: граф, открытый завтра, вернёт тот же промт. Удалённая с тех пор
  запись останавливает прогон и говорит об этом, а не пишет тихонько новый промт:
  граф просил конкретный.

- **Сохранённый промт сверяется с тем, что ноде на самом деле показывают.** Промт
  T2VA самодостаточен, а промт кадровой задачи или Ref2VA называет свои референсы
  и описывает их прямо в тексте, так что переиспользование его на другом наборе
  ассетов описывает то, чего нет. Если род и число разошлись с тем, для чего
  писалась запись, нода скажет об этом - на ноде и в консоли - и всё равно отдаст
  промт: переиспользовать описание как шаблон - законное занятие. Миниатюры на
  карточке закрывают то, чего не поймает никакая проверка: подмену картинки на
  другую того же рода.

- **Кнопки копирования в окне библиотеки** - у каждой записи, у строки Last Prompt
  и рядом с Save в окне сохранения. Кладут промт в буфер обмена, ни на что ноду не
  наводя.

- **`prompt_file` на ноде Options и кнопка `New prompt file` рядом.** Один файл -
  один рабочий набор, `global`, пока воркфлоу не скажет иначе; подключённые к этой
  Options ноды сохраняют в него и листают его. Файлы - обычный JSON в
  `ComfyUI/user/minimax_h3_rewriter/prompts/`, их можно править руками,
  копировать между машинами и удалять. Запись с одной миниатюрой и промтом в
  полную длину весит около 11 КБ.

- **Плашка для `repeat_last` рядом с той, что уже была у `bypass`.** Показывает
  `REPEAT`, когда нода отдаёт собственный последний ответ, и `LIBRARY`, когда ею
  правит сохранённый промт; на свёрнутой ноде показывает тусклое имя
  переключателя, а по клику переключает его - ровно как плашка bypass, с которой
  они теперь делят реализацию. Ноду она не перекрашивает: этим занят bypass, вдвоём
  они встречаются постоянно, и один цвет не может означать оба состояния.

### Изменено

- **`repeat_last` стал единственной ручкой, а окно библиотеки лишь выбирает, что
  именно он отдаёт.** Выбор сохранённого промта включает переключатель;
  выключение возвращает ноду модели и оставляет выбор ждать, а `Write a new one` в
  окне делает и то и другое. До этого сохранённый промт действовал независимо от
  переключателя, и видимая ручка не делала ничего, пока решала невидимая. Теперь
  кнопка так и написана: `Library: <имя>`, а если переключатель выключен, то с
  `(repeat_last is off)` в конце.

## 0.17.3 - 2026-08-29

### Добавлено

- **`repeat_last` на каждой ноде-райтере, рерайтере и капшенере.**
  Переключатель, который отдаёт ответ, выданный нодой в прошлый раз, вместо
  повторного прогона модели. Если ещё ничего не сохранено, нода один раз считает
  по-настоящему, запоминает написанное и говорит об этом; дальше она возвращает
  тот же текст, пока переключатель включён, что бы ни менялось. В этом и смысл:
  пятидесятисекундный рерайт не стоит оплачивать второй раз, пока собирается всё,
  что идёт после него.

  Собственный кеш ComfyUI так не умеет. Ключ кеша ноды - это её класс, значение
  `IS_CHANGED` и все полученные входы, так что правка промта как раз и
  выбрасывает запись, а `IS_CHANGED` умеет только добавлять инвалидацию, но не
  скрывать её. Поэтому ответы лежат в собственном хранилище пака, по одному на
  ноду.

  Хранилище живёт в памяти на время сессии ComfyUI и больше нигде: оно не пишется
  на диск, не путешествует с воркфлоу и очищается перезапуском. Каждый запуск
  честно говорит, откуда взялся ответ, в трёх местах: подпись под нодой, консоль
  ComfyUI и всплывающая подсказка самого переключателя, где указаны время
  сохранения, длина текста и его начало. Если входы разошлись с теми, на которых
  ответ был получен, все три об этом скажут, а не сделают вид, что ничего не
  изменилось. `bypass` по-прежнему главнее.

  На двух нодах-капшенерах хранится именно описание, а не собранный блок:
  нумерация принадлежит цепочке, поэтому строка пишется заново вокруг того, что
  пришло на `previous`, а ассет, подключённый уже после, описывается
  по-настоящему.

### Исправлено

- **На трёх нодах не было отметки и подкраски `bypass`.** Фиолетовый цвет и
  плашка BYPASSED, которыми помечена выключенная нода, рисуются расширением со
  списком типов нод внутри, и этот список не пополнили, когда появились
  рерайтер 8B, рерайтер Omni и Universal Rewriter. Теперь все девять нод с
  переключателем `bypass` выглядят одинаково, свёрнутые и развёрнутые, а плашка
  на свёрнутой ноде переключает его так же, как и везде.

## 0.17.2 - 2026-08-29

### Добавлено

- **`merge_lora` и примерно вдвое больше токенов в секунду на пути через
  safetensors.** PEFT держит подключённый адаптер рядом с базовой моделью и
  считает его на каждом токене, поверх базовых весов; если вплавить его в эти
  веса один раз при загрузке, та же арифметика делается заранее. Замерено на
  Qwen3-VL-8B с 8B-адаптером, 128 токенов на 5090: **14.3 токена в секунду
  подключённым против 25.0 вплавленным** на `bfloat16` и 13.6 против 25.1 на
  `nf4`. Само слияние стоит 0.07 с на неквантованной базе и 4.6 с на базе
  bitsandbytes, которую приходится послойно деквантовать и заквантовать обратно.

  `auto`, значение по умолчанию, берёт бесплатную половину: вплавляет адаптер в
  неквантованную базу и оставляет подключённым на `nf4`/`int8`, где цена реальна,
  как и лишнее округление. `on` вплавляет и там; `off` - поведение, которое было
  до сих пор. Пути GGUF это не касается: llama.cpp применяет адаптер по-своему, и
  `merge_lora` о нём ничего не говорит.

  **Вплавленный запуск - не дословно тот же, что подключённый.** `W + BA`,
  посчитанное один раз, не побитово то же, что `Wx + B(Ax)` на каждом токене:
  где-то выпадает другой токен, а за ним идёт и остаток фразы. На том же seed -
  два расхождения на 128 токенов в `bfloat16` и больше в `nf4`, где PEFT сам
  предупреждает про переквантование. Ни один из ответов не «правильнее» другого.
  Выверенный до токена воркфлоу - повод поставить `off`.

  Стоит записать и то, что проблемой не оказалось, раз уж работа была в замерах:
  повторное использование загруженной модели стоит 0.00 с, токенизатор - 0.28 с,
  процессор - 0.48 с против 6-15 с загрузки, а `device_map`, пришпиленный к одной
  карте, вышел вровень с `"auto"`. Дело было целиком в адаптере.

## 0.17.1 - 2026-08-28

### Добавлено

- **32:9 и 48:9** - под кадр, который шире одного монитора: 32:9 это два экрана
  16:9 рядом, 48:9 - три. Значение едет текстом, одной строкой в ходе, и ни этот
  пак, ни руководства MiniMax его не разбирают, поэтому список свободно растёт.
  Что стоит знать заранее: ни одна лора этих строк в обучении не видела, так что
  модель компонует шире, читая запрос, а не узнавая выученную форму.

  Пикеру пришлось расти вместе со списком. Каждый прямоугольник рисуется по своей
  пропорции в общем габарите, поэтому широкий край читается по тому, сколько
  высоты от него осталось, - и на прежнем габарите 32:9 и 48:9 выходили в 8 и 6
  пикселей, причём второй упирался в ограничитель, и обе пропорции читались
  одинаковой полоской. Габарит стал шире, ограничитель ниже, и 21:9, 32:9 и 48:9
  разошлись примерно на 15, 10 и 6 пикселей. Нода, которой не хватает ширины на
  восемь, переносит ряд на вторую строку и подрастает ровно на неё, а не срезает
  последние прямоугольники.

  Запрошено [@Geese586](https://github.com/Geese586) в
  [#9](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/9).

- **`aspect_ratio` - вход у всех райтеров и ререйтеров**, потому что форму кадра
  обычно решают в другом месте графа и пишут там иначе. Штатный Resolution
  Selector называет 16:9 как `16:9 (Widescreen)`, нода размеров скажет
  `3840x1080`, делитель - `1.78`. Читаются все три, и подпись вокруг пары
  читается насквозь: важна пара чисел, где бы она ни стояла. Размер кадра,
  отстоящий от записи в списке не больше чем на 2%, называется её именем - именно
  это превращает `1376x768` (собственный ответ Resolution Selector для 16:9 при
  1 МП) в `16:9`, а не в `43:24`. Пропорция, которой нет ни в одном списке,
  проходит как есть - так сюда попадают `2.39:1` и `5:4`, - а то, что вовсе не
  пропорция, отклоняется с называнием значения, а не молча компонуется.

  Сокет принимает связь типа `STRING` или `COMBO`, так что примитив, который уже
  управляет Resolution Selector, тянется одним проводом и сюда. Тянуть надо от
  селектора: примитив принимает тип той ноды, к которой подключён первой, а
  примитиву, ставшему STRING, комбо-виджет уже нечего предложить.

  Пока провод подключён, вход главнее пикера, и интерфейс это показывает:
  квадраты гаснут, ничего не горит, клики не проходят. При отключении поле
  очищается - нода сверху пишет своё значение прямо в виджет (так провод и
  кормит виджетный вход), а оставшееся значение продолжало бы главенствовать над
  только что зажёгшимся пикером.

- **Кнопка Open model list на ноде Prompt Rewriter Omni** - единственной в паке,
  где её не было. В подсказке теперь названы `models_8b` и `models_omni` рядом с
  `models`: три ререйтера читают три секции, а кнопка открывает файл для всех.

### Изменено

- **У выбора пропорций больше нет сокета.** `resolution` объявлен `socketless` на
  всех нодах, где он есть: пропорция, приходящая из графа, - это `aspect_ratio`,
  который читает написания, реально используемые другими нодами, а две двери в
  одну настройку означали, что парсер можно обойти. Воркфлоу, где `resolution`
  был превращён во вход, потеряет эту связь при загрузке - входа, к которому она
  вела, больше нет, - и значение, которое она подавала, стоит потом проверить в
  пикере.

### Исправлено

- **Кнопки перестали попадать в промт.** `Open model list` и `Open guide folder` -
  канвасные кнопки без значения, но в экспорт API-формата они всё равно
  доезжали как `"Open model list": null`, то есть входом, которого нода не
  объявляла, - и так в каждом воркфлоу с этим паком. `serialize = false` держит
  виджет только вне сохранённых `widgets_values`; для промта нужен ещё
  `serializeValue`. Незнакомые ключи ComfyUI отбрасывает, так что это был мусор,
  а не поломка.

- **Четыре места, где README расходился с кодом.** Капшенер описывался как не
  требующий рантайма на машине, где хоть раз выполнялся ререйт, - а это верно
  только если тот ререйт шёл через бинарники: `gguf_runtime` по умолчанию берёт
  `llama-cpp-python` везде, где колесо импортируется, а база safetensors llama.cpp
  не касается вовсе, так что для большинства установок за архив платит первый же
  капшен. `llama_backend = auto` был описан как Vulkan на 34 МБ, хотя на Windows
  с картой NVIDIA compute capability 8.6, 8.9, 12.0 или 12.1 он берёт CUDA - 511
  МБ. Распакованный рантайм назывался «собственной папкой `runtime/`», хотя поиск
  идёт по конкретному бэкенду, и vulkan-сборка, распакованная руками, не годится
  для запуска, разрешившегося в CUDA. А в таблице требований `llama-cpp-python`
  значился как то, что нужно для GGUF, - при разделе тремя страницами ниже,
  озаглавленном «ставить ничего не надо».

## 0.17.0 - 2026-08-26

### Добавлено

- **MiniMax-H3 Prompt Rewriter Omni** - под третью лору LightX2V, первую, которая
  слышит. Она обучена на Qwen2.5-Omni-7B, той самой модели, которую капшенеры
  этого пака уже используют, поэтому референс доходит до неё самим собой, а не
  чьим-то предложением о нём: картинкой, клипом, звуком. И только она из трёх
  обучена на **Ref2AV** - задаче полного референса, которая отвечает шестью
  полями вместо трёх.

  Один растущий вход принимает IMAGE, VIDEO или AUDIO, так что неправильного
  гнезда здесь нет: как назовут референс, следует из того, что он такое, а
  картинки нумеруются среди картинок, звуки среди звуков. Стрип под ним
  показывает, что подключено, в каком порядке и как будет названо, - и этот
  порядок и есть нумерация: перетащите вторую картинку в начало, и она станет
  `<Picture 1>`. Переименования здесь намеренно нет, в отличие от стрипа
  Universal Writer: тип входа решает, что такое референс, а *субъект* - это то,
  что эта лора **производит** в `subject_definitions`, а не то, что ей подают.

  `duration` снапится перед тем, как попасть в ход. MiniMax-H3 генерирует по
  сетке 17n+5 кадров на 24 fps, поэтому большинства длительностей просто не
  существует: просишь 10 секунд - получаешь 243 кадра, 10.13 с, и именно это
  число модель повторяет в строке выравнивания. Виджет остаётся тем, что вы имели
  в виду.

- **Omni-лора в формате GGUF** - сконвертирована из safetensors LightX2V через
  `convert_lora_to_gguf.py` из llama.cpp и опубликована в
  [pytraveler/MiniMax-H3-Prompt-Rewriter-LoRA-Omni-GGUF](https://huggingface.co/pytraveler/MiniMax-H3-Prompt-Rewriter-LoRA-Omni-GGUF)
  в `F16` (0.65 ГБ) и `Q8_0` (0.34 ГБ, ведёт себя так же). А значит, Omni-ререйтер
  работает по пути llama.cpp вообще без установки пакетов, при загрузке 6.2 ГБ
  вместо 22.4 ГБ, которые стоит база в safetensors.

- **`models_omni` и `adapters_omni` в `models.json`** - базы Qwen2.5-Omni-7B в
  обеих формах, в которых опубликована лора. Два похожих, но неподходящих
  варианта помечены в списке, а не спрятаны: Qwen2.5-Omni-**3B** - это
  `(wrong size for the adapter)`, а **Qwen2.5-VL-7B** - та же строка
  архитектуры, те же 28 блоков, та же ширина, то есть лора *прикрепится*, - это
  `(vision only, not an Omni build)`; различает их отсутствие аудио-энкодера в
  проекторе.

- **Третья вкладка в Universal Rewriter, и Ref2VA вместе с ней.** Тот же промпт,
  те же два кадра, третья лора, один клик. Вкладке принадлежат `model_omni` и
  `quantization_omni`, всё выше - общее, как и раньше.

  Вкладка приносит и пятую задачу, и два гнезда под неё: `reference_video` и
  `reference_audio`, которые читает только `Ref2VA` и больше никто - у двух
  других лор нет слуха, а четыре кадровые задачи берут одни картинки, поэтому
  звук, подключённый к `FL2VA`, отклоняется по имени, а не выбрасывается молча.
  На `Ref2VA` всё подключённое становится референсом в порядке гнёзд:
  `first_frame`, `last_frame`, клип, звук.

  Четыре её новых выхода дописаны **после** тех трёх, что заполняет любая задача,
  а не вставлены между ними: ComfyUI адресует связь выхода индексом слота, и
  `subject_definitions` посередине сдвинул бы `overall_soundscape` во всех уже
  собранных на этой ноде графах. Та же ловушка, что с позициями виджетов, только
  этажом ниже.

  Четыре референса и без стрипа, тогда как нода Prompt Rewriter Omni берёт
  двенадцать и даёт их перетаскивать: порядок - это и есть всё правило нумерации,
  а при четырёх гнёздах порядок задаётся самими гнёздами.

### Исправлено

- **Удалённая из списка моделей секция остаётся удалённой.** Удаление записей по
  одной работало и раньше: `seed_offered` помнит каждое имя, которое поставляемый
  список когда-либо предлагал, поэтому убранное не возвращается. А вот удаление
  секции целиком - нет: слияние читало отсутствующую секцию как «эта установка
  старше неё» и копировало поставляемую целиком, при каждом запуске и навсегда.
  То есть секция оказалась единственной правкой, которую файл не сохранял, - в
  файле, весь смысл которого в том, что он ваш и его можно править.

  Две ситуации различаются по той же записи, по которой различаются записи
  внутри. Установке, которая старше секции, её содержимое никогда не предлагали,
  и она получает всё; тому, кто секцию удалил, предлагали каждую запись, и он не
  получает ничего. Нашлось при проверке, доезжают ли `models_omni` и
  `adapters_omni` до уже существующего пользовательского файла - доезжают, а это
  лежало под ними.

- **Референсный кадр больше не приезжает во vision-башню в полный размер.** Qwen
  тратит токен на блок 28x28, поэтому картинка 1616x1616 - ровно то, что
  воркфлоу ComfyUI отдаёт не задумываясь, - это 3249 токенов. Две таких
  переполняли контекст 8k раньше, чем считалось хоть одно слово промпта, и
  `FL2AV` падал на `decode: failed to find a memory slot for batch of size 1316`,
  успев закодировать обе. `I2AV` с одной картинкой выживал - из-за чего это
  выглядело сломанной задачей, а не исчерпанным бюджетом.

  Размер плох не только этим. Скрипты инференса самой LightX2V ставят потолок
  картинке на процессоре - 301056 пикселей для Omni, 1024x1024 для 8B, - то есть
  полноразмерный кадр это ещё и форма, которой ни одна из моделей не видела при
  обучении. Обе ноды теперь масштабируют до потолка своей лоры, а кадру из клипа
  потолок ниже - 100352 пикселя, по той причине, по которой клип это много
  картинок.

  Контекст после этого считается по факту - по написанным на диск файлам, а не
  на глаз, - поэтому `Ref2AV` с восемью референсами расширяет его, а не падает.

- **База Omni в safetensors больше не отвергается как не та модель.** Проверка,
  читающая `config.json`, заглядывала на один уровень - в `text_config`. А
  Qwen2.5-Omni держит языковую модель на два уровня ниже, в
  `thinker_config.text_config`, потому что в чекпоинте лежат ещё talker и
  вокодер, - так что наверху нет вообще никакого `hidden_size`, и база, на
  которой лора и обучена, объявлялась «не Qwen2.5-Omni-7B». Теперь проверка
  спускается вниз.

- **И она теперь грузится.** У `AutoModelForImageTextToText` нет записи для
  `qwen2_5_omni` - только для thinker'а внутри, той половины, под которую лора и
  вырезана, и единственной, которая вообще пишет текст. То есть даже пройдя
  проверку выше, загрузка упала бы на «unrecognized configuration class».
  Загрузчик спускается по тем же вложенным конфигам и берёт класс из собственной
  таблицы Transformers.

- **Забитый KV-кэш больше не выдаёт себя за нечитаемый проектор.** Строки `find a
  memory slot` не было среди тех, что распознаёт подсказка об ошибке, поэтому она
  проваливалась в заметку про форматы проекторов и отправляла читателя менять
  модель там, где чинилось одно число.

## 0.16.6 - 2026-08-25

### Добавлено

- **Каждому референсу можно задать свой вопрос.** Под каждым квадратом стрипа
  появилась узкая полоса того же цвета: тёмная, пока референс спрашивают
  обычным вопросом его роли, и подсвеченная, как только его спрашивают о другом.
  Клик открывает окно с текстом, ПКМ очищает, наведение показывает текст в
  подсказке. Именно на референс, а не на ноду: одна нода описывает разом и
  картинку, и клип, и звук, а одно поле на всех заставило бы спросить у всех
  троих одно и то же.

  **Два режима, и полоса говорит какой.** `+ instr` дописывает строку к вопросу
  роли; `= instr`, на сплошной подложке, спрашивает *вместо* него. Нужны оба, и
  ни один не может быть единственным: «не упоминай окно» — это правило, и
  спрошенное само по себе оно не оставляет ничего, что просило бы описание, —
  модель отвечает правилу, словом «No». «Всегда пиши "blah blah blah"» — это
  целый вопрос, и оставленный перед ним вопрос роли заставляет маленькую модель
  разрешить противоречие в пользу описания. Замена снимает и строку длины: кто
  пишет вопрос, тот владеет и формой ответа.

  Текст едет в скрытом JSON-виджете, поэтому путешествует вместе с воркфлоу и
  через API, как и любая другая настройка. Голая строка там читается как
  дописывание — это безопасная половина для всего, что написано руками.

  Попросил [@808charlie](https://github.com/808charlie) в
  [#8](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/8).

- **`system_prompt` у райтеров** - то, что нацеливает их на что-нибудь помимо
  MiniMax-H3. Собранный гайд заменяется целиком тем, что вы туда напишете, и
  тогда гайд даже не скачивается: системный промпт, написанный под LTX, Krea или
  Wan, превращает эти ноды в райтеры под них, офлайн и без документа, который
  всё равно не прочтут. Короткий путь - нода `MiniMax-H3 Guide Prompt (any LLM)`:
  она отдаёт штатный промпт выходом, возьмите, поправьте и верните обратно.

  Сообщение задачи не заменяется никогда - в нём промпт, соотношение сторон и
  длительность, которые нужны любому гайду. А ответ по-прежнему разбирается на
  секции H3, поэтому гайд, отвечающий абзацем, наполнит `rewritten_prompt` и
  оставит выходы секций пустыми.

  Попросил [@808charlie](https://github.com/808charlie) в
  [#8](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/8).

### Исправлено

- **Рассуждения капшенера больше не попадают в блок референсов.** CLIP-капшенер
  всегда вырезал блок `<think>` из ответа, GGUF-капшенер не вырезал никогда -
  поэтому каждую подпись открывал пустой `<think> </think>`, а думающая модель
  вписывала в `reference_assets` все свои рассуждения целиком, откуда они ехали
  дальше, в промпт райтера. Теперь вырезают оба пути.

  Одного выреза мало, и иначе быть не может: райтеры сами рендерят шаблон чата и
  передают `enable_thinking=False`, а подпись так не умеет - шаблон применяет сам
  `llama-mtmd-cli`, именно это и ставит медиа-токены на нужное место, и никакого
  переключателя для канала размышлений он не публикует. Значит, рассуждения
  по-прежнему тратят бюджет `--predict`: так думающая модель выдаёт страницу
  размышлений и половину предложения подписи. Поэтому каждый вопрос капшенера
  теперь заканчивается просьбой дать только описание - единственный инструмент,
  который у этого пути есть.

  Сообщил [@808charlie](https://github.com/808charlie) в
  [#8](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/8).

### Изменено

- **Multi Reference Caption рисует тот же стрип, что и Universal Writer.** Его
  переключатели рисовались прямо на канвасе, который рендерер Nodes 2.0 не
  рисует вовсе; квадраты - это HTML, который рисуют оба. Значит, механизм теперь
  один, и у ноды, где были галочки, появились цветные квадраты: клик по квадрату
  заглушает его референс, а подпись говорит, как он будет назван и каким по
  счёту. Перетаскивания среди них нет - эта нода пишет блок в порядке самого
  гайда, а имя референсу даёт группа, в которую он воткнут.

- **Новые виджеты добавляются только в конец ноды.** ComfyUI восстанавливает
  значения виджетов по позиции, поэтому виджет, добавленный в любое другое
  место, отдаёт каждому виджету ниже значение соседа: воркфлоу, сохранённый до
  обновления, вернулся с `max_frames`, в котором лежало значение `context_size`.
  В релиз это не попало, но записать стоит - потому что очевидное место для новой
  настройки как раз рядом с той, к которой она относится.

## 0.16.5 - 2026-08-24

### Исправлено

- **Капшенер больше не резервирует весь контекст, на котором модель обучали.**
  `--ctx-size 0` читается как "пусть llama.cpp решит сама", а означает "контекст,
  на который модель обучена", и упакованные капшенеры эту разницу скрывали:
  Qwen2.5-Omni просит 32k и помещается где угодно. Qwen3-VL просит 262144
  токена, а её кэш - это 36 слоёв по 8 KV-голов размерности 128, K и V, в f16:
  144 КиБ на токен, то есть 36 ГиБ KV-кэша, выделяемых до того, как прочитан
  первый пиксель. На карте в 32 ГБ запуск падал на `failed to allocate buffer
  for kv cache`, так и не посмотрев на картинку, а собственный auto-fit
  llama.cpp его не спасал, потому что нода жёстко задаёт `--n-gpu-layers`.

  Теперь 0 означает "сколько нужно этому запуску и не больше, чем поместится в
  карту". Длина контекста и форма кэша читаются из заголовка GGUF, который скан
  моделей и так разбирает, и соотносятся с числом референсов и памятью
  устройства, куда запуск направлен. Число, вписанное в `context_size`,
  по-прежнему выполняется буквально, а заголовок, из которого посчитать нечего,
  как и раньше оставляет решение за llama.cpp.

  Сообщил [@808charlie](https://github.com/808charlie) в
  [#7](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/7).

### Изменено

- **Упавший капшенер говорит, в какую сторону он упал.** Заметка про форматы
  проекторов печаталась при любом ненулевом коде выхода `llama-mtmd-cli`, и
  нехватка памяти возвращалась в костюме модели, которую mtmd не умеет читать, -
  отправляя читателя изучать модель в тот момент, когда потомок уже сказал
  `cudaMalloc failed`. У невлезшего выделения и у слишком тесного под кадры
  контекста теперь свои ответы, с именем виджета, который их двигает, а заметка
  про проектор осталась тем выходам, которые действительно о проекторе.

- **Скан капшенеров перестал вести репортаж.** Модель, лежащая в одной папке с
  mmproj, к которому её не удалось привязать, печатала строку с перечислением
  всех проекторов этой папки, уровнем INFO. Плоский `models/LLM` с дюжиной
  посторонних квантов и пятью проекторами объявлял двенадцать моделей по пять
  имён каждая - и четырежды, по разу на каждую ноду с выпадающим списком
  капшенеров, при каждом запросе описаний нод от ComfyUI. Теперь одна строка на
  папку, уровнем DEBUG.

  Сообщил [@808charlie](https://github.com/808charlie) в
  [#7](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/7).

## 0.16.4 - 2026-08-24

### Изменено

- **Установленный `llama-cpp-python` больше не выглядит так, будто он должен был
  избавить капшенер от скачивания.** Не должен: референс уходит в
  `llama-mtmd-cli`, отдельную программу, а колесо - это набор разделяемых
  библиотек, которые Python грузит через ctypes, и исполняемых файлов в нём нет
  вовсе. В том числе в CUDA-сборке из исходников: она даёт `libllama.so` с
  соседями и ни одной CLI-цели. Поэтому машина с полностью рабочим колесом всё
  равно качала архив релиза ради подписи к картинке, и единственным объяснением
  были 32 МБ трафика.

  Теперь капшенер говорит об этом до скачивания, а не после, - в лог, и ещё раз
  в тексте отказа, если качать нечего. `gguf_runtime` относится к нодам-райтерам;
  у капшенеров пути через колесо нет вообще.

- **`llama_backend = cuda` под Linux объясняет, что считается вашей сборкой, а
  что нет.** Отказ и раньше говорил, что CUDA-архива под Linux upstream не
  публикует, а собранный вами llama.cpp запускается как есть. Не сказано было
  главное: `llama-cpp-python` такой сборкой не является, с какими флагами его ни
  собери, - а это первая же мысль у того, кто только что собрал колесо с
  `-DGGML_CUDA=on` и ждёт того же от капшенеров. Теперь названы две цели cmake,
  которые дают недостающие программы, и `llama_bin.txt` рядом с
  `MINIMAX_H3_LLAMA_BIN` и `PATH`. В капшенере к отказу добавился и блок
  "где искали", и называет он в нём `llama-mtmd-cli`, а не бинарник ререйтера,
  которого никто не искал.

- **Скачивание рантайма, которому не добраться до GitHub, печатает, как сделать
  это руками.** Ссылки на архивы, папка, куда их распаковать, и единственное,
  что тихо ломается при распаковке файловым менеджером: разделяемые библиотеки
  лежат symlink'ами и должны ими остаться. Пачка одинаковых ошибок соединения -
  не совет для машины, которая офлайн намеренно.

  Сообщил [@808charlie](https://github.com/808charlie) в
  [#7](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/7).

## 0.16.3 - 2026-08-22

### Изменено

- **GGUF, который не загрузился, теперь объясняет причину.** llama-cpp-python
  поднимает на любую неудачу загрузки одно и то же - `Failed to load model from
  file:` и путь, - а собственное объяснение llama.cpp уходит в stderr уровня C,
  который этот бэкенд глушит. Так что самая частая поломка приходила вообще без
  причины: модель, чья архитектура новее, чем llama.cpp, вкомпилированный в
  установленное колесо. Qwen3-VL требует сборку от 2025-10-30 и новее, Qwen3.5 -
  от 2026-02-10, а llama-cpp-python 0.3.16 несёт в себе llama.cpp от 2025-08-14:
  обычный Qwen3 там грузится, а каждое более новое семейство падает одинаково -
  и выглядит это как сломанная нода.

  Теперь отказ называет архитектуру, прочитанную из заголовка самого файла, и
  говорит, есть ли под неё загрузчик в этой сборке - по таблице архитектур,
  вкомпилированной в библиотеку. Иначе этого не узнать: C API не публикует ни
  таблицу, ни номер сборки, и версия пакета тоже ни о чём не говорит - форки
  нумеруют себя сами, а любую версию можно собрать с любым llama.cpp.
  Для урезанной или упакованной библиотеки, где поиск ничего не доказывает,
  остаётся общее сообщение: обвинять не того нельзя.

  Сообщил [@808charlie](https://github.com/808charlie) в
  [#4](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/4).

## 0.16.2 - 2026-08-22

### Исправлено

- **Рантайм llama.cpp распаковывается под Linux и macOS.** Проверка перед
  распаковкой tar отклоняла любой symlink без разбора, а официальные архивы для
  Linux и macOS кладут разделяемые библиотеки ровно как SONAME-symlink'и -
  `libllama.so.0 -> libllama.so.0.0.10310`, по десять штук в каждой сборке под
  Linux и восемнадцать в сборке под macOS, - так что первая же загрузка умирала
  на `refusing archive member outside the target: 'llama-b10310/libllama.so.0'`,
  и рантайм не доставался вообще ни одной машине кроме Windows. Windows это не
  задевало никогда: там архивы zip, и у той ветки своя проверка.

  Эти ссылки здесь не украшение. `ldd llama-completion` разрешает
  `libllama.so.0`, `libggml.so.0` и `libggml-base.so.0` - то есть сами ссылки, -
  так что оставить файлы и выбросить ссылки значило бы распаковать рантайм,
  который загрузчик всё равно не запустит.

  Ссылка теперь отклоняется, только если её цель действительно выходит за
  пределы папки назначения - ровно тот zip-slip, ради которого проверка и
  стоит. Оба вида ссылок разрешаются оттуда же, откуда их разрешает `tarfile`:
  цель symlink'а - относительно папки самой ссылки, цель hardlink'а -
  относительно корня распаковки. Одна база на оба вида считала бы глубину самой
  ссылки дважды и пропустила бы `build/bin/x -> ../../secret` - два уровня выше
  папки назначения - как если бы это был `secret` внутри неё. Абсолютная цель
  отклоняется сразу.

  Найдено и исправлено [@ViolinKaine](https://github.com/ViolinKaine) в
  [#3](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/pull/3),
  на релизе b10310 под Linux.

## 0.16.1 - 2026-08-22

### Добавлено

- **Путь в файле — для сервера, чьё окружение задаёте не вы.** Одна строчка в
  `ComfyUI/user/minimax_h3_rewriter/llama_bin.txt` — папка, в которой собран ваш
  llama.cpp, или сам бинарник, — и нода запускает эту сборку. Файл читается
  после `MINIMAX_H3_LLAMA_BIN` и раньше всего остального.

  Именно этого `MINIMAX_H3_LLAMA_BIN` не может на сервере. `export` доходит до
  сервера, запущенного из этой же оболочки, и больше никуда: юнит systemd,
  entrypoint контейнера или скрипт-лаунчер выдают процессу собственное окружение
  и `~/.bashrc` не читают, так что переменной там просто нет — как нет и того,
  что вы тем же способом дописали в `PATH`. Файл читает сама нода, и ей всё
  равно, кто запустил ComfyUI.

### Изменено

- **Если llama.cpp не нашёлся нигде, отказ печатает, что было у этого процесса на
  самом деле.** Переменная и стоит ли она здесь, файл и есть ли он, папка
  распакованного рантайма и `PATH` со счётом записей — а под Linux ещё и та самая
  команда, которая показывает настоящее окружение работающего сервера. «Положите
  в PATH» — бесполезный совет тому, кто ровно это и сделал в оболочке, которой
  сервер не видел, так что сообщение показывает поиск, а не повторяет указание.

## 0.16.0 - 2026-08-21

### Добавлено

- **llama.cpp, который на машине уже есть, запускается как есть, и ничего не
  скачивается.** Прежде чем тянуть официальные бинарники, нода теперь смотрит в
  три места по порядку: путь из `MINIMAX_H3_LLAMA_BIN` — сам исполняемый файл
  или папка, где он лежит, — затем собственная папка `runtime/`, затем `PATH`.

  Именно этого не хватало собственной сборке. `llama_backend` называет архив,
  который надо скачать у upstream, а под Linux upstream CUDA-архива не публикует
  вовсе — так что `llama_backend = cuda` там был тупиком независимо от того, что
  стоит на машине, включая CUDA-сборку llama.cpp прямо в `PATH`: в `PATH` никто
  не смотрел. Теперь она находится; после этого `llama_backend` перестаёт что-либо
  значить, потому что он выбирает загрузку, а не то, с чем скомпилирован уже
  имеющийся бинарник; и `device = cuda:0` начинает делать то, что написано.

  Ноды-описатели берут `llama-mtmd-cli` оттуда же: в любой сборке он лежит рядом
  с бинарником дополнения. Сборка, где есть одно и нет другого — обычно это
  пакет дистрибутива, — отправит ноду за архивом ради одной этой задачи, а не
  откажет.

  Переменная, указывающая в пустоту, вызывает ошибку, а не проваливается в
  загрузку: назвать сборку — это указание, и опечатка не должна стоить
  полугигабайтного архива.

### Изменено

- Подсказка у `llama_backend` и оба README говорят, где ищется рантайм, а отказ
  на `llama_backend = cuda` под Linux заканчивается тем, что с этим делать, а не
  только тем, чего upstream не публикует.

## 0.15.0 - 2026-08-21

### Добавлено

- **«MiniMax-H3 Universal Rewriter» - обе лоры-ререйтера в одной ноде, а
  вкладка выбирает, которая запустится.** Две существующие ноды-ререйтера не
  изменились и по-прежнему регистрируются.

  Вкладке принадлежат `model` и `quantization`, по паре на адаптер, и больше
  ничего. Промт, задача, соотношение сторон, продолжительность, seed и оба
  кадровых входа общие, потому что для обеих лор значат одно и то же, - так что
  попробовать второй адаптер стоит одного клика, а не второй ноды, которую надо
  вручную держать в согласии с первой. Виджет чужой вкладки прячется, а не
  сбрасывается, и переживает сохранение с загрузкой: он всё ещё стоит там, куда
  вы его поставили.

  Переключатель задачи тоже общий, и вкладка 27B его не трогает: на ней горит
  T2VA, три кадровые задачи погашены, а клик не делает ничего - значение,
  выставленное на вкладке 8B, дождётся возврата. Запустите вкладку 27B с
  подключёнными кадрами - нода сама скажет, что не читает их и куда их деть.

  Два входа IMAGE, `first_frame` и `last_frame`, у каждого чекбокс на своей
  строке, и выключенная строка считается неподключённой.

  Captioner'а на вкладке 27B нет, и это проверено, а не предположено. Описание,
  вложенное в промт, до адаптера доходит и обученную форму не ломает, - но
  картинка растворяется в сцене, а не прибивается к отметке 0.00, ровно как
  написано на странице самой лоры: T2VA закрыт, FL2VA нет. Виджет под это
  обещал бы кадровую задачу, которой 27B не умеет.

- Чекбоксы кадровых строк, полоса вкладок, переключатель задачи и выбор
  пропорций рисуются тем же кодом, что и у Universal Writer: он переехал в
  `web/js/mmx_controls.js`, а не был скопирован.

### Изменено

- **Переключатель задачи у Universal Writer гасит каждую задачу, которую полоса
  не может обеспечить, а не один Ref2VA.** Считаются квадраты с бейджем `pic` -
  тот самый счёт, на котором нода и так отказывает: при пустой полосе горит один
  `T2VA`, одна картинка зажигает `I2VA` и `L2VA`, две - `FL2VA`, а три гасят все
  три, потому что три для `I2VA` так же невозможны, как ноль. Погашенная кнопка
  несёт ту фразу, которую выдал бы запуск, так что решение читается одинаково до
  и после. Считаются бейджи, а не сокеты: переключите картинку в субъекты - и
  задача, которую она блокировала, загорится.

- Обвязка движка у каждого ререйтера теперь функция, а не метод, и нода её
  вызывает. В каком формате база, какой адаптер к ней идёт, какой из трёх
  движков запускается - одна копия, вызываемая из двух нод.

### Исправлено

- Виджет, спрятанный скриптами этого пака, теперь спрятан в **обоих**
  рендерерах нод. Классический канвас читает `widget.hidden`, а рендерер
  Nodes 2.0 - `options.hidden`, и выставлялся только первый: карта чекбоксов на
  Multi Reference Caption рисовалась текстовым полем под *Modern Node Design* -
  ровно тем виджетом, вместо которого чекбоксы и сделаны.

## 0.14.1 - 2026-08-21

### Добавлено

- **Ререйтер 8B работает и на сборке safetensors, а не только на GGUF.** Адаптер
  издан как `adapter_model.safetensors` в
  `lightx2v/MiniMax-H3-Prompt-Rewriter-LoRA-8B` и обучен на официальной папке
  `Qwen/Qwen3-VL-8B-Instruct`, а нода до сих пор умела дотянуться только до
  GGUF-конвертации того и другого. Теперь список базовых моделей предлагает обе
  формы, и нода выбирает движок по ней.

  Что этот путь даёт - резидентность. База в GGUF с кадрами идёт через
  `llama-mtmd-cli`, каждый раз новым процессом, поэтому `keep_model_loaded` мог
  работать только для T2VA; safetensors грузится в сам процесс ComfyUI через
  Transformers и PEFT и остаётся там для любой задачи. Чего стоит - загрузки:
  17.5 ГБ базы и 2.8 ГБ адаптера против 4.7 + 0.7 + 0.7.

- **`quantization` на ноде 8B** - тот же виджет, что у 27B, и по той же причине:
  `nf4` требует около 8 ГБ VRAM, `int8` около 13, `bfloat16` около 20. Для базы
  в GGUF игнорируется: там своя квантизация.

- **Кнопка "Open model list" на двух нодах, где её не было** - на ререйтере 8B и
  на Multi Reference Caption. Обе берут модель из `models.json`, как и все
  остальные, так что причин её не показывать не было никаких.

### Изменено

- **Проверка базовой модели знает, какой адаптер сейчас будут применять.** Она
  сравнивала четыре числа из `config.json` с константами, которые всегда
  описывали Qwen3.6-27B, и потому могла отвечать только за 27B. Теперь эти
  четыре числа ездят вместе как `Shape`, и вызывающий говорит, о каком адаптере
  речь. Qwen3-VL-4B отклоняется для адаптера 8B по имени и числам -
  `hidden_size is 2560, the adapter needs 4096`, - а не после загрузки. Для 27B
  не меняется ничего: он передаёт свою форму.

- Движок на transformers разделён на подготовку входа и его прогон, чтобы
  мультимодальный путь появился без второй копии сэмплинга, стриминга и
  обработки прерывания. Процессор чекпоинта грузится и кэшируется рядом с
  моделью, а у текстового чекпоинта его просто нет.

- **Секции с адаптерами наконец доходят до вашего `models.json`.** `adapters` и
  `adapters_8b` — словари, а слияние, которое держит списки актуальными, есть
  теоретико-множественная операция над именованными записями *списка*, так что до них оно
  никогда не доходило: ни одна запись об адаптере никогда не попадала ни в чей файл.
  Чтение при этом работало — ненастроенная запись откатывается к пакетному значению, —
  но строки, которую можно нацелить на свою конвертацию, не было, а ведь ради этого
  файл и отдаётся вам. Теперь они сливаются поформатно и записываются в
  `seed_offered` под именем секции: формат, изданный позже, приходит, а удалённый
  вами намеренно — остаётся удалённым.

- Шаблон чата, лежащий на токенизаторе, а не на процессоре, теперь используется,
  а не приводит к отказу. Qwen3-VL-4B - как раз такой чекпоинт, и шаблон его
  токенизатора расставляет те же плейсхолдеры под картинки.

## 0.14.0 - 2026-08-21

### Добавлено

- **MiniMax-H3 Universal Writer** - одна нода на весь кадр: референсы, их
  порядок и сам ререйт. Она покрывает то же, что Multi Reference Caption и обе
  ноды-писателя вместе, сразу для всех пяти задач. Те три не изменились и
  остаются в меню, так что ничего из уже собранного не ломается.

  Складывать их в одну есть смысл ради порядка. `Picture 1` и `Picture 2` в
  FL2VA не взаимозаменяемы - одна открывает видео, другая закрывает, - а до сих
  пор единственным, что решало, какая из них какая, был порядок, в котором их
  записала caption-нода, а он брался из того, в какой слот что воткнули. Вещь
  настоящая, несущая - и нигде на экране не видная.

  Один сокет принимает картинку, клип или звук, слоты добавляются по мере
  заполнения. Метку и номер референс получает на полосе под входами: тащите
  квадратик, чтобы переставить; кликните по подписи, чтобы сказать, чем служит
  картинка (`pic`, `subj`, `vid`); кликните по номеру, чтобы выключить референс,
  не выдёргивая провод. Так один сокет всё равно даёт все четыре метки, которые
  разрешает Ref2VA, а различие, которое Multi Reference Caption проводит
  четырьмя группами входов, здесь проводится на квадратике.

  Проверено в ComfyUI 0.30 на каждой задаче. FL2VA и I2VA с двумя картинками;
  Ref2VA с картинкой, второй картинкой, переназначенной в субъект, и звуковым
  клипом на том же растущем сокете - вернулось как `Subject 1`, `Video 1` и
  `Audio 1` в порядке полосы; T2VA с подключёнными референсами, которые
  правильно проигнорированы, вообще без загрузки описателя. Перестановка на
  полосе меняет порядок блока. Смена подписи на квадратике меняет и инструкцию
  описателю, и метку, под которой записан ответ. Задача, которой полоса не
  соответствует, отклоняется до любой загрузки и скачивания - как и Ref2VA со
  всеми выключенными референсами. Сохранённый и заново открытый воркфлоу вернулся
  со всеми значениями виджетов на местах.

- **Полоса референсов, переключатель задачи и выбор пропорций - рисованные
  контролы**, на HTML через `addDOMWidget`, а не на канвасе. Это единственный
  механизм виджетов, который ComfyUI отрисовывает и в классическом канвасе, и в
  рендерере Nodes 2.0, и Vue для него не нужен: фронтенд написан на Vue, но Vue
  наружу не отдаёт, а его собственный реестр Vue-виджетов - закрытый список.

  Каждый контрол забирает себе обычный виджет, объявленный нодой, и сохраняет
  его имя и его место. Воркфлоу хранит значения виджетов по позиции, поэтому
  контрол, вставший *рядом* с заменяемым виджетом, а не вместо него, сдвинул бы
  все значения после себя. А браузер, который скрипт так и не загрузил,
  откатывается к текстовому полю и двум выпадающим спискам, и нода продолжает
  работать.

  Из правой панели Parameters они убраны флагом `hideInPanel` - тем же, что
  ставят собственные Load3D и text-preview виджеты ComfyUI. Панель выбирает
  компонент как `getComponent(widget.type) || WidgetLegacy`, ветки для DOM-виджета
  там нет, и нарисовать она может только пустую строку с подписью - да и не смогла
  бы лучше: показать один HTML-элемент в панели значит снять его с ноды.

- **`duration` на новой ноде - слайдер**, с точностью до десятой секунды. Его
  верхний предел задаёт собственное свойство ноды `max_duration` (правый клик,
  Properties Panel), по умолчанию 30 секунд. Диапазон виджета фиксируется при
  объявлении ноды, и одно число не подходит всем графам, поэтому сервер принимает
  до десяти минут, а слайдер охватывает то, чем граф реально пользуется. На
  остальных нодах `duration` остаётся целыми секундами, как и был.

### Изменено

- `guide_prompt` принимает дробную длительность. Строка задачи пишется как `10s`
  и `7.5s`, а не `10.0s`, чтобы целое число секунд по-прежнему выглядело целым.
  Все существующие ноды передают целые секунды, и на них это не сказывается.

- Чекбоксы на строках входов вынесены в общий код. `web/js/slot_switches.js`
  теперь держит рисование, попадание мышью и арифметику колонки, которые раньше
  нёс один `multi_caption_switch.js`, а обе ноды настраивают его под себя -
  каждая со своим представлением о том, каким слотам нужен чекбокс и где лежит
  состояние. Поведение Multi Reference Caption не изменилось.

  Пока этот код переезжает, стоит знать - и это было верно и до релиза: чекбоксы
  рисуются на канвасе, а экспериментальный режим ComfyUI "Modern Node Design
  (Nodes 2.0)" выходит из `LGraphCanvas.drawNode` до того, как отработает хоть
  один пер-нодовый колбэк. В этом режиме они ни рисуются, ни кликаются - ни на
  одной из двух нод. Квадратики новой ноды дают доступ к тому же состоянию, и это
  одна из причин, почему на каждом из них есть и переключатель вкл/выкл.

- Пустая подпись теперь сообщается, а не проходит молча. Метка всё равно попадает
  в блок: полоса обещала этот номер этому квадратику, и закрыть пробел значило бы
  сделать её лгуньей, - но голое `Subject 1:` говорит писателю, что ассет есть, и
  не говорит, какой. Теперь нода называет слот на экране и в логе. Замечено на
  думающем текстовом энкодере через вход `clip`, где ответ оказывается блоком
  рассуждений, который так и не закрывается.

## 0.13.0 - 2026-08-19

### Добавлено

- **MiniMax-H3 Prompt Rewriter 8B (sees frames)** - вторая нода ререйтера, для
  нового адаптера LightX2V на Qwen3-VL-8B-Instruct. Она мультимодальна: там, где
  27B нужно словами рассказать, что на референсном кадре, этой показывают сам
  кадр - и строку выравнивания она пишет по тому, что видит. Четыре задачи -
  T2VA, I2VA, FL2VA, L2VA - против одной у ноды 27B, при ~6.1 ГБ загрузки и
  ~9 ГБ VRAM.

  Движка два, и выбирает задача. В T2VA картинок нет, и она идёт обычным
  текстовым путём: проектор не загружается, а `keep_model_loaded` работает.
  Остальные три несут кадры и идут через `llama-mtmd-cli` - это каждый раз новый
  процесс, и удержать модель он не может; нода пишет об этом в лог, а не молча
  игнорирует переключатель.

  Проверено на всех четырёх задачах на Qwen3-VL-8B-Instruct Q4_K_M с адаптером
  F16. T2VA сразу начинает с трёх полей; остальные три открываются строкой
  выравнивания дословно в обученной формулировке. С выключенным `use_lora` та же
  модель по-прежнему заполняет три поля - контракт лежит в системном промте, а не
  только в LoRA, - но перестаёт писать маркеры кадров `[Shot 2]` и отвечает
  примерно втрое короче. Это и есть видимая работа адаптера.

- **`adapter` стал выпадающим списком.** В нём обе LoRA в обеих опубликованных
  точностях плюс все `.gguf`-адаптеры, уже лежащие в папках моделей ComfyUI, - с
  архитектурой в подписи, чтобы `qwen35` и `qwen3vl` различались. Раньше выбор
  кванта означал правку `models.json` руками, а это путь для продвинутых, а не
  интерфейс. Первая запись - прежняя строка по умолчанию, символ в символ, и
  означает "ту сборку, которая подходит выбранной базовой модели": сохранённые
  воркфлоу сохраняют свой выбор и на любой из двух нод берут нужный адаптер.

  Что при этом потеряно: в поле больше нельзя вписать текст, поэтому на
  самостоятельно сконвертированную LoRA теперь указывают, положив файл в
  `models/LLM`, а не написав путь. Путь, который уже лежит в сохранённом
  воркфлоу, по-прежнему разрешается.

- **`models_8b` и `adapters_8b` в списке моделей** - базовые модели 8B (Q4_K_M и
  Q8_0, каждая со своим проектором) и адаптер 8B. Отдельные списки, потому что
  запись из любого семейства не загрузилась бы в чужой ноде. Оба подмешиваются в
  существующий `models.json` при обновлении, а прежняя копия, как всегда,
  остаётся рядом как `.bak`.

### Исправлено

- **Папка с несколькими моделями и одним проектором подставляла его всем.**
  `models/LLM` с десятком не связанных между собой GGUF и единственным `mmproj`
  удовлетворяла правилу "проектор один, значит он и есть нужный", и в списке
  капшенеров каждая модель из папки предлагалась в паре с ним - текстовая 27B с
  проектором, собранным для 8B, которая загрузится и начнёт писать бессмыслицу,
  а не упадёт. Теперь короткий путь работает, только если и модель в папке одна;
  иначе имена должны совпадать - как это уже было при нескольких проекторах.

- **Сборка Q8_0 адаптера для 27B была не видна тем, кто установил пак раньше, чем
  она появилась.** `adapters` - это словарь, а слияние, которое поддерживает
  списки моделей в актуальном состоянии, работает как алгебра множеств над
  именованными записями *списка*, и до словаря просто не доходило. Теперь
  поставляемые `alternatives` подмешиваются в живой список, если тот указывает на
  тот же репозиторий и своих не перечисляет, - в том числе в список, написанный до
  переименования аккаунта: старый идентификатор опознаётся как та же публикация.

### Изменено

- В поставляемом списке конвертированные GGUF-адаптеры теперь названы
  `pytraveler/...`: аккаунт переименован из `ivanfromm`. Hugging Face
  перенаправляет старый идентификатор, так что править ничего не нужно и уже
  существующие списки продолжают работать.

- `llama-cli` теперь запускается с `-st`. Это запасной бинарник, он используется
  только когда нет `llama-completion`, и на Qwen3-VL он входит в режим чата
  вопреки `-no-cnv` и ждёт второго хода - то есть нода зависает, а не падает. На
  `llama-completion`, который работает на самом деле, флаг не меняет ничего:
  побайтово одинаковый вывод на Qwen2.5-Omni-3B и на Qwen3-VL-8B.

## 0.12.1 - 2026-08-17

### Исправлено

- **LoRA молча не применялась на сборках llama-cpp-python, перешедших на новый
  API адаптеров.** Бэкенд передавал адаптер как `Llama(lora_path=...)` - так это
  пишется в апстриме с самого начала. Сборки, заменившие его реестром -
  `load_lora(name, path)` при загрузке, затем `active_loras=[{"name": ...,
  "scale": ...}]` на каждый вызов - этого параметра больше не имеют, а
  `Llama.__init__` принимает `**kwargs`, который никогда не читает, так что
  аргумент не отвергался, а принимался и выбрасывался. Ничего не падало, лог
  по-прежнему сообщал об адаптере, а нода отвечала от чистой базовой модели:
  `use_lora` включённая и выключенная давали побайтово одинаковый текст, с точками
  склейки в виде диапазонов `00:00-00:05` базовой модели вместо тренированной
  формы `00:08.500`.

  Теперь то, какой API у сборки, определяется чтением сигнатуры - потому что
  отказ здесь выглядит как молчание, и ловить нечего. Заменённая проверка на
  `TypeError` не могла сработать в принципе: `**kwargs` есть в каждой сборке,
  включая апстрим, просто там это не имело последствий. Сборка, у которой нет ни
  одного из двух API, теперь отказывается работать и указывает на
  `gguf_runtime: llama.cpp`, чей `--lora` никогда не был затронут, вместо того
  чтобы тихо считать неадаптированную модель.

  Регистрация не предполагается, а перечитывается через `list_loras()`.
  `Llama.eval` ищет адаптер по имени и при промахе пропускает его с
  предупреждением, которое печатается только при `verbose` - а этот бэкенд его
  выключает, так что непроверенный `load_lora` воспроизвёл бы ровно ту же тишину
  этажом ниже. Успешная регистрация теперь пишет одну строку `INFO` с именем
  адаптера и числом загруженных.

  Найдено, разобрано до строки и проверено
  [@ioritree](https://github.com/ioritree) в
  [#2](https://github.com/pytraveler/MiniMax-H3-Prompt-Rewriter-ComfyUI/issues/2),
  на CUDA-форке JamePeng версии 0.3.47.

- **Access violation при выгрузке, когда адаптер наконец действительно
  подключён.** Тот же реестр обходится в `LlamaModel.close` *после* освобождения
  базовой модели, поэтому `llama_adapter_lora_free` работает по уже исчезнувшей
  памяти, и рабочий поток ComfyUI умирает с `access violation reading 0x...` на
  каждой выгрузке. Адаптеры теперь освобождаются, пока модель, в которую они
  ссылаются, ещё жива: `close` получает пустой реестр и в плохую ветку не
  заходит. Дважды ничего не освобождается - собственный `free` адаптера проверяет
  свой указатель, - а у сборок без нового API этого метода просто нет, и их это
  не касается.

## 0.12.0 - 2026-08-16

### Добавлено

- **Вход `clip` у обеих caption-нод: описание моделью, которая у ComfyUI уже
  загружена.** Маршрут через GGUF поднимает `llama-mtmd-cli` по процессу на
  ассет, поэтому кадр с пятью референсами читает веса с диска пять раз.
  Подключите вместо этого мультимодальный текстовый энкодер из `CLIPLoader` - и
  та же работа происходит внутри ComfyUI, на модели, которую держит его
  собственный аллокатор: прочитана один раз, переиспользована на каждый ассет и
  ещё раз на следующем запуске. Замерено на Gemma-4 12B: три ассета - картинка,
  клип и его звуковая дорожка - за 24 с, и один `Requested to load` в логе на них
  всех.

  Этим закрывается вопрос о запуске `llama-mtmd-cli` в чат-режиме ради той же
  экономии. Он означал бы управление интерактивным процессом через stdin и
  выскабливание приглашения из stdout - против рантайма, у которого собственное
  правило гласит, что stdin потомка закрыт. Маршрут через энкодер даёт ту же
  экономию с документированным API и вовсе без подпроцесса, поэтому идея
  чат-режима снята.

  Ничего не заменяется. `model` остаётся ровно таким, как был, и просто не
  читается, пока подключён `clip`; оставьте вход пустым - и обе ноды ведут себя
  как прежде, вплоть до формулировок ошибок.

- **Возможности энкодера проверяются до запуска** - та же любезность, которую
  маршрут через GGUF оказывает, читая заголовок проектора. Ищется при этом
  аудио*проектор*, а не аудио*энкодер*, и разница не косметическая: у Gemma-4 E2B
  и E4B есть настоящий аудиоэнкодер, а 12B - это сборка 'unified' без энкодера
  вовсе, которая проецирует аудиокадры прямо в языковую модель, и искать там
  нечего. Проба по энкодеру объявила бы глухой модель, которая прекрасно слышит.
  Gemma-4 31B, которая действительно не слышит, отклоняется - как и Qwen3-VL.

- **Рассуждения срезаются с капшена.** Попросить модель не думать - не то же
  самое, что получить отсутствие мыслей: декодер Gemma-4 переписывает свой канал
  размышлений в `<think>`/`</think>`, а fp8-сборка E4B заполняет этот канал
  страницей анализа, хотя промпт примирует его закрытым. Капшен - это одна строка
  референсного блока, поэтому всё до закрывающего тега отбрасывается, а вместе с
  ним и незакрытый блок, который остаётся от оборванного ответа.

  Две связанные находки про Gemma-4 E2B/E4B, обе воспроизводятся штатной нодой
  ComfyUI `Generate Text` на том же чекпоинте и потому чинятся не здесь: она
  размышляет вслух, как описано выше, и аудио до неё не доходит - капшен
  возвращается словами о том, что клип не предоставлен. Увидев энкодер такой
  формы, нода пишет предупреждение в лог, а не отказывает: на другом чекпоинте
  это может и работать. Gemma-4 12B принимает звук правильно - именно на ней и
  сделан замер выше.

### Изменено

- **`media` раскладывает VIDEO не только в PNG, но и в батч IMAGE.** Оба приёмника
  делят сэмплирование, стратегию перемотки и `max_frames`, поэтому клип,
  описанный любым из маршрутов, описан по одним и тем же кадрам. Кадры доходят до
  энкодера как `image=`, а не `video=`, и это намеренно: у токенизатора Qwen3-VL
  нет аргумента video, и кадры были бы молча проигнорированы, а видеопуть Gemma-4
  пересэмплирует до 1 fps, что тихо перебило бы `max_frames`.

## 0.11.0 - 2026-08-15

### Добавлено

- **Третий выход у `Guide Prompt (any LLM)`: оба промпта одной строкой.** Почти
  любая LLM-нода принимает ровно один промпт, поэтому подключение этой всегда
  требовало ноды склейки строк посередине. Повод - штатная нода `Generate Text`,
  появившаяся в 0.30: она гоняет языковую модель внутри процесса ComfyUI, взяв
  чекпоинт из `CLIPLoader`. Связка `CLIPLoader` плюс `Guide Prompt` плюс
  `Generate Text` оказывается самым коротким путём к формату этого пака вообще
  без загрузок - если текстовый энкодер Qwen3-VL или Gemma-4 уже лежит на диске
  ради картиночной модели.

  Чтобы `Generate Text` заработала, нужны три настройки, и README их называет:
  `max_length` заметно выше значения по умолчанию 512, потому что это бюджет
  вывода, а шесть секций Ref2VA в него не помещаются; `thinking` выключить; и
  `use_default_template` не трогать.

- **`format` там же, `plain` или `chatml`.** `plain` склеивает два промпта пустой
  строкой и оставляет применение шаблона LLM-ноде, из-за чего руководство
  оказывается внутри пользовательского хода - работает везде, поэтому и по
  умолчанию. `chatml` расписывает ходы здесь: токенизатор Qwen пропускает свой
  шаблон, как только текст начинается с `<|im_start|>`, и руководство приходит
  настоящим системным сообщением. В этой же ветке пропускается подавление
  размышлений у Qwen, поэтому пустой блок think дописывается следом - по той же
  причине, по которой маршрут через GGUF рендерит шаблоны с
  `enable_thinking=False`. Это заточено под Qwen по устройству, и об этом
  сказано прямо.

  Новый выход добавлен в конец, новый виджет опционален, поэтому сохранённый
  воркфлоу сохраняет все свои значения и все свои связи.

## 0.10.0 - 2026-08-15

### Добавлено

- **`MiniMax-H3 Multi Reference Caption` - все референсы кадра в одной ноде.**
  Цепочка нод описания точна, но разрастается: пять референсов - это пять нод,
  пять проводов и пять шансов оставить где-нибудь не ту роль. У этой ноды
  виджета `role` нет вовсе: метка задаётся тем, в какую группу воткнут ассет.
  Это словарь самого руководства, ставший структурой. Ref2VA определяет ровно
  четыре референсные метки и запрещает выдумывать новые, поэтому `subjects`,
  `pictures`, `videos` и `audios` покрывают формат полностью, а описать картинку
  как звук становится невозможно, а не просто нежелательно. Слоты растут по мере
  заполнения, один свободный всегда наготове - это `io.Autogrow` из v3-API нод
  ComfyUI.

  Блок выходит в порядке руководства - субъекты, картинки, видео, аудио, - а не
  в порядке проводов, и каждая метка по-прежнему нумеруется внутри своей
  категории, продолжая то, что пришло на `previous`, поэтому нода спокойно стоит
  в цепочке с одиночными нодами по обе стороны. `model`, `length`, `seed`,
  `max_frames`, `context_size` и `bypass` общие для всех её ассетов.
  `description` и `instruction` не перенесены: текст, написанный руками,
  относится к одному ассету за раз, и у одиночной ноды они остались.

- **Чекбокс у каждого референсного слота, на строке самого слота.** Капшен стоит
  загрузки модели и от секунд до минут, поэтому «всё, кроме вот этого» - самое
  обычное желание, а выдёргивать ради него провод значит терять ту самую
  разводку, ради которой всё и делалось. Выпадающий список имён был бы дешёвым
  решением и неверным: вся ценность в том, чтобы нажать переключатель того
  входа, на который смотришь. Штатно ComfyUI виджет туда не положит - фронтенд
  поднимает все чистые сокеты выше всех виджетов, в каком бы порядке их ни
  просила схема, - поэтому чекбокс рисуется на ноде по высоте самой строки, а
  клик ловится с канваса. Само состояние - обычный скрытый виджет с JSON: именно
  это позволяет ему пережить сохранение и дойти до бэкенда через API, как любое
  другое значение. Записываются только выключенные слоты, так что у нетронутой
  ноды в сохранённом воркфлоу пусто. Фронтенд, до которого скрипт не доехал,
  оставит поле с JSON видимым, и всё продолжит работать.

- **Слот видео принимает и `VIDEO`, и батч `IMAGE`.** Загрузчики видео
  расходятся в том, что именно отдают: `Load Video (Upload)` из
  VideoHelperSuite отдаёт кадры, а не `VIDEO`. Референс при этом один и тот же,
  поэтому слот принимает оба варианта, а запуск сам разбирается, что пришло.
  Кадры в обоих случаях сэмплируются равномерно до `max_frames`, так что
  стоимость клипа не зависит от его длины.

### Изменено

- **Пак больше не регистрируется по принципу «всё или ничего».** Новой ноде
  нужен свежий ComfyUI ради растущих входов, поэтому она регистрируется
  отдельно, и установка, слишком старая для v3-API нод, теряет только её одну, а
  не все ноды пака из-за единственного неудачного импорта.

## 0.9.5 - 2026-08-14

### Добавлено

- **`bypass` у четырёх нод, которые запускают модель.** У ререйтера на LoRA, у
  обоих ведомых писателей и у ноды описания референса появился переключатель,
  который пропускает модель целиком: ничего не качается, ничего не грузится,
  VRAM не трогается. Писатели отдают `prompt` прямо в `rewritten_prompt` - это
  дешёвый способ сравнить написанный промпт с исходным, ничего не перетыкая;
  нода описания пропускает `previous` без изменений, то есть убирает из цепочки
  один ассет, оставив саму цепочку собранной. Нумерация это переживает: каждая
  нода нумерует то, что получила, поэтому ассеты после выключенного смыкаются.
  Выходы-секции возвращаются пустыми.

  Штатный bypass ComfyUI, Ctrl+B, здесь так не умеет: он пробрасывает
  подключённую связь и больше ничего, а все входы, из которых пишут эти ноды, -
  виджеты. Нажать Ctrl+B на ререйтере значит не пробросить ничего, и ноды ниже
  по графу останутся ни с чем. На ноде описания это работает наполовину - связь
  `previous` пробрасывается, - и ломается на первой ноде цепочки, у которой
  никакого `previous` нет. Переключатель стоит последним виджетом, поэтому
  workflow, сохранённый до этого релиза, сохраняет все свои значения.

- **Бейдж над заголовком ноды и фиолетовая нода в состоянии bypass.** Свёрнутая
  нода рисует только тайтл-бар - и это ровно то состояние, в котором виджет
  недоступен, а свернуть workflow и выключить один тяжёлый шаг хочется. Бейджи
  LiteGraph рисует над заголовком независимо от состояния ноды, а его хит-тест
  перебирает бейджи с обработчиком клика, прежде чем сдаться, поэтому бейдж
  нажимается и на свёрнутой ноде, и ради этого не пришлось патчить ни одного
  обработчика канваса. Цвет - это то, что читается на зуме, где на экране весь
  workflow; он подменяется под собственным геттером цвета ноды, а не пишется в
  саму ноду - так же, как это делает штатный bypass ComfyUI, - поэтому он не
  попадает в сохранённый workflow, а нода, покрашенная вручную, остаётся со
  своим цветом. Фронтенд, слишком старый для бейджей, теряет бейдж и сохраняет
  переключатель, в котором сама возможность и живёт.

## 0.9.4 - 2026-08-10

### Добавлено

- **`trust_remote_code`, выключен.** Чекпоинт для Transformers может нести
  собственный код модели - на него указывает `auto_map` в `config.json`, - и
  загрузка такой модели импортирует и выполняет этот Python с правами вашего
  пользователя. Ни одна модель из поставляемого списка так не делает: все записи
  `transformers` - это варианты Qwen3.6-27B, а записи GGUF до Transformers
  вообще не доходят. То есть для предлагаемых нодой моделей переключатель не
  меняет ничего. Он существует ради модели, которую вы добавили в `models.json`
  сами: нода останавливается и говорит об этом, вместо того чтобы выполнить код,
  а включение переключателя - это ваше заявление о том, какой модели вы
  доверяете.

### Изменено

- **`adapter` отклоняет сетевой путь.** Все остальные модели выбираются из
  выпадающего списка, поэтому сохранённый воркфлоу несёт в себе *название*
  записи и никогда - путь; `adapter` сделан текстовым полем, потому что указать
  на `.gguf`-LoRA собственной конвертации люди действительно хотят, - а значит,
  скачанный воркфлоу может это поле заполнить. `\\host\share\...` и
  `//host/share/...` отклоняются до того, как их кто-нибудь прочитает: сам взгляд
  на такой путь - это попытка аутентификации на указанном хосте. Своя шара
  по-прежнему доступна по букве диска, а путь из `models.json` не ограничен
  вовсе.
- **Применённый адаптер пишется в лог** при каждом запуске, а если это не тот
  адаптер, который настроен, - на уровне warning. Подменённая LoRA иначе
  невидима: нода всё так же отрабатывает и всё так же заполняет все поля, просто
  пишет что-то другое.

## 0.9.3 - 2026-08-09

### Добавлено

- **Запись в `models.json` может указывать на файл, который уже лежит на диске.**
  В `repo` вместо идентификатора Hugging Face можно дать папку на этой машине,
  либо положить весь путь в `file`, а `repo` не писать - работают обе формы и во
  всех трёх секциях, файл читается там, где лежит: ничего не скачивается и
  никуда не копируется. Несуществующий путь называется и отклоняется, а не
  обходится загрузкой.

### Исправлено

- **Сломанный `models.json` был самым тихим отказом во всём паке.** Разбор падал,
  вместо списка подставлялся упакованный, и выпадающий список выглядел обычно -
  правка, которая не применилась, была неотличима от правки, которая ничего не
  меняет. Теперь первая запись каждого списка моделей несёт саму ошибку разбора
  со строкой и колонкой; если выбрать её и нажать Run, сообщение повторится и
  назовёт файл, который надо починить. Остальной список на месте, ComfyUI
  работает.

## 0.9.2 - 2026-08-08

### Исправлено

- **Звуковая дорожка из видеозагрузчика отклонялась.** Собственный AUDIO у
  ComfyUI - обычный `dict`, но это нигде не закреплено, и распространённые
  загрузчики этого не соблюдают: VideoHelperSuite отдаёт `LazyAudioMap` -
  `Mapping`, который запускает ffmpeg при первом чтении ключа, - а
  `isinstance(audio, dict)` отвечает на него "нет". Теперь спрашиваются два
  ключа, а не тип контейнера, - и именно чтение ключей заставляет ленивый вход
  декодироваться, так что это должно происходить там, а не в проверке
  принадлежности.
- **VIDEO мог подвесить запуск навсегда.** `llama-mtmd-cli --video` передаёт файл
  в `ffprobe` через *stdin*, и если у MP4 атом `moov` лежит в начале - а это и
  есть "faststart", и именно так пишут ComfyUI, телефоны и почти весь веб, -
  ffprobe получает всё нужное за несколько килобайт и выходит, не дочитав
  остальное. llama.cpp в этот момент всё ещё пишет оставшиеся мегабайты в эту
  трубу и остаётся там навсегда: ни вывода, ни ошибки, ни конца. Тот же ролик с
  `moov` в конце отрабатывает за шесть секунд. Кадры теперь декодируются внутри
  процесса - на длинных роликах переходом сразу к нужному кадру, причём именно к
  кадру, а не к его ключевому: иначе восемь выборок при GOP в 250 кадров
  схлопнулись бы в четыре.
- **`max_frames` теперь что-то значит и для VIDEO** - именно это делает стоимость
  описания ролика независимой от его длины: две секунды при 25 fps - это 56
  изображений через визуальную башню, а тридцать секунд - 750.

### Добавлено

- Скриншоты ноды-писателя T2VA, писателя Ref2VA, цепочки Reference Caption и ноды
  опций в обоих README, с замеренным на них временем.

## 0.9.1 - 2026-08-08

### Добавлено

- **`device` - `auto`, `cpu` или по одному `cuda:N` на каждый GPU, который видит
  ComfyUI**, одним написанием для всех трёх бэкендов (`--device CUDA1` для
  бинарников llama.cpp, `main_gpu` для llama-cpp-python, `device_map` для
  Transformers). Важно здесь не размещение: **на другой карте собственные модели
  ComfyUI больше не выгружаются первыми.** Каждый бэкенд выгружал их безусловно -
  что правильно, когда обеим моделям нужна одна и та же VRAM, и чистое
  расточительство, когда нет: после каждого рерайта диффузионная модель
  загружалась заново. Выберите вторую карту - и `keep_model_loaded` наконец
  становится осмысленным. Устройство, которого в машине нет, отклоняется, а не
  молча заменяется другим.

### Исправлено

- **Список ваших GGUF-моделей стоил 31 секунды на десять файлов.** Для
  выпадающего списка нужны шесть значений из первых килобайт каждого файла, но
  `gguf.GGUFReader` разворачивает весь заголовок сразу при открытии - вместе с
  `tokenizer.ggml.tokens`, четвертью миллиона строк, - и счёт приходил при первом
  же ответе ComfyUI на `/object_info`. Теперь заголовок обходится напрямую и
  пропускается: те же шесть значений за **0.4 с**. Недокачанный файл по-прежнему
  отклоняется - сверкой смещений тензоров с размером файла, а не неудачей при
  попытке их отобразить.

## 0.8.1 - 2026-08-08

### Изменено

- **Модели, добавленные в пак после установки, подмешиваются в ваш список.** Ваша
  копия по-прежнему никогда не перезаписывается, но "мы не тронем ваш список"
  тихо превращалось в "вы никогда не увидите ничего нового", и нигде не было
  сказано, что нода знает о большем. Правило - теория множеств, а не сверка
  версий: рядом со списками ваш файл хранит `seed_offered`, все названия, которые
  упакованный список когда-либо показывал этой установке, и обновление добавляет
  только то, что есть в паке, но нет ни в вашем файле, ни среди уже предложенного.
  Удалённая запись остаётся удалённой, переименованная не задваивается,
  по-настоящему новая приходит. Одно исключение, однократное: файл, написанный до
  появления этого механизма, не помнит, что ему предлагали, поэтому при первом
  обновлении вернётся всё недостающее, а прежний файл останется рядом как
  `models.json.bak`. Файл, который нода не может разобрать, остаётся ровно таким,
  какой он есть.

## 0.8.0 - 2026-08-08

### Добавлено

- **MiniMax-H3 Reference Caption.** Ноды писателя читают текст, а не пиксели - вот
  откуда берётся этот текст. Подключите картинку, звук или видео, и небольшая
  мультимодальная модель опишет их одной размеченной строкой `reference_assets` -
  3 с на кадр, 2 с на звуковой фрагмент, 5 с на видео на Qwen2.5-Omni-3B весом
  3.4 ГБ. Работает через те же бинарники llama.cpp, что и всё остальное, так что
  машина, на которой уже прошёл хоть один рерайт, не скачивает никакого рантайма.
- **Цепочка проводами**: `reference_assets` в `previous` следующей ноды. Каждая
  метка нумеруется внутри своей категории - это собственное правило руководства, -
  поэтому четыре ассета выходят как `Picture 1`, `Picture 2`, `Video 1`,
  `Audio 1`, а не с первого по четвёртый.
- **`role` выбирает и метку, и задаваемый вопрос.** Важнее всего вопрос для
  `Audio`: `<Audio N>` - обычно эталон *тембра*, а расшифровка выбрасывает ровно
  ту часть, которая нужна, поэтому инструкция прямо говорит "не расшифровывать" и
  просит голос, манеру, инструменты и фон.
- **Список `captioners` в `models.json`**, с полем `mmproj` рядом с `file` -
  мультимодальная модель это два файла из одной конвертации. В списке только те
  пары, которые действительно запускались: `mtmd` в llama.cpp должен понимать
  формат проектора, и у Gemma 4 он обрушивает процесс целиком, тогда как
  `llama-completion` гоняет ту же модель как текстовую без единой жалобы.

## 0.7.0 - 2026-08-08

### Добавлено

- **Ноды писателя - T2VA/I2VA/FL2VA/L2VA и Ref2VA.** Те же выходные поля без LoRA
  и без 27B: собственное руководство MiniMax по написанию промптов уходит в
  системный промт, и пишет по нему любая GGUF, умеющая следовать инструкциям.
  Минимальная рабочая конфигурация падает с ~10 ГБ и ~13 ГБ VRAM до **2.6 ГБ и
  ~5 ГБ**, а вместе с ней приходят четыре задачи, которых LoRA не умеет вовсе. У
  Ref2VA шесть секций вместо трёх, включая определения субъектов и анализ
  сохранения признаков.
- **MiniMax-H3 Guide Prompt** - отдаёт тот же системный промт любой языковой
  модели, которая у вас уже есть, для тех, кто предпочитает запускать модель сам.
- **Руководства скачиваются из репозитория MiniMax, а не поставляются в паке**,
  поэтому обновление руководства - это обновление результата без релиза здесь.

## 0.6.2 - 2026-08-08

### Добавлено

- **Форма базовой модели проверяется до того, как что-то скачано.** Qwen3.5-9B
  несёт в заголовке ту же `general.architecture = qwen35`, поэтому выглядит
  подходящей и попадала в список моделей, - но у неё 32 блока шириной 4096 там,
  где адаптеру нужны 64 по 5120, и llama.cpp отказывается прицепить LoRA. Нода
  читает эти два числа из заголовка первыми и говорит об этом. Если 9B выдаёт
  правдоподобный рерайт - она работает **без** адаптера: формат приходит из
  системного промта, а не из LoRA.
- **`gguf_runtime` - `auto`, `llama-cpp-python`, `llama.cpp`** - отделяет *чем
  запускается модель* от `llama_backend`, который выбирает, *какую официальную
  сборку скачать*. Принудительный выбор колеса теперь падает с внятным
  сообщением, если его нет, а не подставляет молча что-то другое; принудительный
  выбор бинарников - выход, когда установленное колесо сломано. Только колесо
  умеет `keep_model_loaded`: бинарники возвращают модель операционной системе,
  как только завершается подпроцесс.

## 0.6.0 - 2026-08-07

Первый публичный релиз, разошёлся без тега.

- Нода ререйтера: на входе короткий промпт на любом языке, на выходе три поля
  H3 - `integrated_multimodal_description`, `overall_soundscape`,
  `non_diegetic_music`. Полностью локально.
- LoRA LightX2V Prompt Rewriter на Qwen3.6-27B в `nf4`, `int8` или `bfloat16` -
  либо на GGUF-кванте через llama.cpp, с бинарниками, которые скачиваются при
  первом запуске.
- Веса, адаптер и рантайм скачиваются по необходимости в собственные папки
  моделей ComfyUI, с прогрессом на ноде.
- Собственный `models.json`, который копируется при первом запуске и никогда не
  перезаписывается обновлением.
